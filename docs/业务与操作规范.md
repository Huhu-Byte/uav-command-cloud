# 业务与操作规范

> 适用项目：上云 API 无人机巡检系统私有化部署。
>
> 这份文档回答四个问题：系统要做什么、谁可以操作、操作后应该发生什么、目前还缺什么。它是后续开发、测试和验收的共同依据。

---

## 一、当前项目真实状态

目前已完成的是**可运行的模拟原型**：有指挥中心、任务管理、事件管理和 Spring Boot 后端；模拟设备状态、任务进度与告警会通过 WebSocket 实时推送到网页，告警可填写处理结果并确认。

目前还没有接入真实无人机、机库、气象站、视频流或大疆上云 API，也没有 MySQL 持久化。因此，当前网页中的设备状态、航线、任务和告警仍是模拟数据，不能用于真实飞行操作。

---

## 二、优先级与开发顺序

| 优先级 | 范围 | 为什么先做 |
|---|---|---|
| P0（第一阶段） | 实时监控、在线运维、设备状态与告警、设备控制、上云 API 接入 | 这是系统能“看到设备、知道是否安全、再安全地操作设备”的基础 |
| P1（第二阶段） | 航线规划、任务设定 | 有了可靠设备数据后，才能安全安排自动飞行 |
| P2（第三阶段） | 远程智能管控、成果查看 | 依赖前两阶段的数据、任务和飞行记录 |

> P0 内部也要按顺序完成：先接入数据和状态，再做告警，再做控制。不能先做控制按钮，否则网页看起来能控制，实际却没有安全判断。

---

## 三、功能需求与操作规范

### 1. 实时监控（P0）

**要解决的问题**：操作人员无需到现场，也能看到作业实况。

**系统应具备的能力**：

- 同时展示多路实时视频，例如不同无人机或不同机库摄像头。
- 可选择某一路放大查看，并显示设备名称、直播状态和当前时间。
- 视频断开时明确显示“视频连接中”或“视频已中断”，不能显示旧画面冒充实时画面。

**操作规则**：

1. 用户进入“实时监控”后选择需要查看的设备。
2. 系统先检查用户是否有查看权限，再请求视频播放地址。
3. 用户只查看视频时，不应触发飞行或设备控制。

**验收标准**：在测试设备正常推流时，网页可稳定展示至少一路实时画面；断流后 10 秒内给出明显提示。

**当前差距**：已完成实时监控前端页面、通道切换结构和断线状态展示；当前仍是模拟画面占位，尚无真实视频流地址。

---

### 2. 在线运维（P0）

**要解决的问题**：无需前往现场，即可了解机库、飞行器和挂载设备是否可用。

**系统应具备的能力**：

- 查看机库门、充电、网络、温度等状态。
- 查看无人机在线状态、电量、定位、飞行状态和最近上报时间。
- 查看挂载设备的连接和工作状态。
- 状态数据必须带“最后更新时间”；超过设定时间未更新时显示“数据过期”。

**操作规则**：

1. 运维人员先查看设备状态，再决定是否创建任务或执行控制。
2. 页面只显示已由后端确认的数据，不直接相信浏览器自己生成的数据。
3. 发现离线、低电量或数据过期时，系统必须给出醒目提示。

**验收标准**：测试设备状态变化后，网页在 5 秒内更新；设备超过 60 秒没有状态上报时标记为异常或离线。

**当前差距**：网页已接收模拟后端的实时无人机状态，并完成设备管理列表、详情、离线提示和数据过期判断；仍没有真实设备、机库状态和挂载状态。

---

### 3. 设备状态检测与告警（P0）

**要解决的问题**：提前发现不适合飞行或设备异常的情况，避免冒险起飞。

**系统应具备的能力**：

- 检测无人机、机库和气象站的状态。
- 识别大雨、大风、低电量、网络中断、机库异常等情况。
- 自动创建告警，记录发生时间、设备、等级、原因和处理状态。
- 告警分为提示、一般、高、紧急四级；高和紧急告警需要在指挥中心明显展示。

**操作规则**：

1. 高或紧急天气告警存在时，系统不允许新任务起飞。
2. 已在飞行中的任务遇到高风险天气时，后端先执行预设安全策略，例如返航或就近降落；网页只负责展示状态和请求人工确认。
3. 运维人员处理告警时必须填写处理结果，保留处理人和处理时间。

**验收标准**：模拟大风或强降雨数据后，30 秒内出现告警；尝试起飞时系统明确阻止并说明原因。

**当前差距**：后端已根据模拟风速生成一条“一般”告警，网页可确认并展示处理记录；仍未接入真实气象站、多规则判断、MySQL 持久化或高风险自动阻止逻辑。

---

### 4. 设备控制（P0）

**要解决的问题**：通过网页安全地远程控制机库、无人机和挂载，不依赖现场遥控器。

**可控制范围**：

- 机库：开关舱门、充电、重启或状态刷新。
- 无人机：起飞、返航、降落、暂停任务、继续任务。
- 挂载：按设备能力进行拍照、录像、变焦或切换工作模式。

**安全操作规则（必须执行）**：

1. 只有被授权的“飞行操作员”或“管理员”可以看到控制按钮。
2. 起飞、降落、返航、开关舱门等高风险操作必须二次确认，并显示操作对象与当前状态。
3. 每次控制前，后端必须检查设备在线、电量、气象、机库状态和当前任务状态。
4. 前端不能直接向无人机发送命令，必须通过后端转发并记录。
5. 每次控制都要写入操作日志：谁在何时对哪个设备执行了什么操作，结果是否成功。
6. 控制失败时必须显示失败原因，不能只显示“已发送”。

**验收标准**：在测试环境中，已授权用户可完成一次安全检查通过的控制；未授权用户无法发起控制；所有操作可在日志中查到。

**当前状态**：已完成返航演示控制。网页通过后端请求返航；后端会检查操作人、设备在线状态、电量、高风险天气、当前任务和重复返航请求，并返回清晰的成功或失败原因。操作人点击返航后必须在二次确认窗口核对设备、任务进度、电量和安全检查说明，确认后才发送请求。正常返航会冻结任务进度、逐步降至 `0 m`，并保留最近操作记录。返航操作记录和告警确认记录已保存到本机 H2 文件数据库，服务重启后仍可查询；当前为模拟环境，已提供飞行操作员、管理员和只读访客三种演示身份，后端会校验控制身份，未登录或只读访客不能发起返航或确认告警。此机制仅用于演示，正式环境必须替换为真实登录、令牌和权限系统。正式共享部署时可通过环境变量切换至 MySQL，建表脚本会自动执行。

---

### 5. 航线规划（P1）

**要解决的问题**：在地图上制作可复用的飞行路线。

**系统应具备的能力**：

- 在 2D 地图上绘制、编辑、保存航线；3D 地图作为后续增强项。
- 支持高空摄影、自定义航点飞行等模式。
- 为每个航点配置高度、速度、拍摄动作和挂载参数。
- 保存前检查路线是否完整、航点参数是否合理。

**操作规则**：航线必须有名称、所属区域、创建人和最后修改时间；修改已被任务使用的航线时，系统要提示可能影响后续任务。

**验收标准**：用户可创建一条至少含三个航点的路线，保存后能再次打开并编辑。

**当前差距**：未开发。前端导航中存在入口，但没有规划功能。

---

### 6. 任务设定（P1）

**要解决的问题**：让无人机按现在或未来的时间规则自动执行航线任务。

**系统应具备的能力**：

- 选择航线、执行设备、开始时间和负责人。
- 支持立即执行、指定时间执行和按天/周重复执行。
- 展示任务状态：草稿、待执行、执行中、已完成、失败、已暂停。
- 记录每一次实际执行的开始、结束、失败原因和飞行记录。

**操作规则**：创建任务不等于立即起飞。真正执行前，后端仍需要进行设备和气象安全检查。

**验收标准**：可创建一次性和重复性任务；系统能在指定时间产生待执行任务；安全检查不通过时任务不执行并留下原因。

**当前差距**：前端已支持任务列表、状态筛选、新建表单和详情查看；新建任务当前只保存在本次浏览器会话中，尚未接入后端保存、调度、时间规则或真实执行能力。

---

### 7. 远程智能管控（P2）

**要解决的问题**：根据设备状态和气象数据自动执行安全动作，减少人工盯守。

**系统应具备的能力**：

- 综合电量、风速、降雨、网络、机库和任务状态进行判断。
- 自动决定允许起飞、暂缓任务、返航或降落。
- 所有自动决定要保留原因、规则版本和执行结果，方便追溯。

**操作规则**：自动控制只能在已配置的安全规则范围内执行；管理员可以暂停自动管控，但不能绕过高风险天气限制。

**验收标准**：模拟高风险天气时，系统能阻止待起飞任务；模拟低电量时，系统能将飞行任务切换到安全返航流程。

**当前差距**：未开发。当前只有返航演示安全检查，尚无自动任务调度、天气联动和多设备智能管控。

---

### 8. 成果查看（P2）

**要解决的问题**：飞行结束后，人员能在线查看采集的照片和视频。

**系统应具备的能力**：

- 自动关联任务、飞行记录、照片、视频和采集时间。
- 支持按任务、设备、时间查看成果。
- 支持图片预览和视频播放；文件不存在或同步失败时显示明确状态。

**操作规则**：成果文件只读保存；删除或导出需要权限并保留记录。

**验收标准**：一次测试任务结束后，至少能在任务详情中查看关联的测试照片或视频记录。

**当前差距**：已完成成果中心前端页面、照片/视频筛选、演示预览和 CSV 清单导出；尚未接入真实媒体文件、播放地址、原文件下载和后端成果归档。

---

### 9. 上云 API 与私有化通信（P0）

**要解决的问题**：把大疆设备数据可靠、安全地接入自建的私有化系统，并向网页提供实时数据。

**总体通信路径**：

```text
无人机 / 机库 / 气象站
        ↓
DJI Cloud API（MQTT、HTTPS）
        ↓
本项目后端（Spring Boot）
        ↓
MySQL（历史数据） + 实时缓存（最新状态）
        ↓
WebSocket
        ↓
Vue 网页（指挥中心、监控、告警、控制页面）
```

**协议使用规则**：

| 协议 | 在本项目中的用途 |
|---|---|
| MQTT | 接收设备持续上报的状态、任务事件、告警等实时消息 |
| HTTPS | 查询配置、下发需要请求响应确认的业务指令、调用管理接口 |
| WebSocket | 后端将最新状态、告警和任务变化实时推送给网页 |

**接入规则**：

1. 大疆密钥、数据库密码和设备凭证只能放在后端环境变量中，不能写入前端代码或提交到 Git。
2. 后端负责验证收到的消息、保存关键历史记录，并把可展示的数据推送给前端。
3. 前端通过 `/api/v1/` 获取历史数据，通过 WebSocket 接收实时变化；前端不直接保存或暴露大疆密钥。
4. 任何控制命令都要有唯一编号、超时处理和结果回执，防止重复发送或“以为成功”。
5. 通信中断、消息格式错误和认证失败都要记录到系统日志，并在运维页面可查。
6. DJI API 地址、客户端编号和密钥只能由服务器环境变量注入；真实地址必须使用 HTTPS，日志不得记录 URL、凭证或异常正文。
7. 真实设备接入必须先走只读状态与告警验证；在完成测试设备验收前，不允许复用只读重试入口发送返航、起飞或降落命令。

**验收标准**：先使用模拟设备消息验证整条链路：后端收到一条设备状态后，网页在 5 秒内更新；之后再替换为真实 DJI Cloud API 凭证与设备。

**当前差距**：已完成模拟状态与告警的 WebSocket 链路、统一数据格式、历史记录数据库和真实 API 的服务器配置边界；仍未取得测试设备、官方认证说明，也未接入 MQTT 或真实 DJI HTTPS 接口。

---

## 四、第一阶段的最小交付目标

第一阶段不追求一次做完所有功能，而是先完成一条可验证的安全链路：

1. 后端启动，并可接收一条模拟设备状态。
2. 网页指挥中心不再使用固定演示数据，而是显示后端推送的在线状态、电量和更新时间。
3. 后端根据模拟风速或降雨产生一条告警，网页实时显示。
4. 网页提供“返航”演示控制按钮；后端先检查权限和安全条件，再返回成功或失败结果并写操作日志。
5. 上述流程稳定后，再使用真实 DJI Cloud API 和设备替换模拟数据。

---

## 五、每次开发后的记录要求

- 前端界面、视频展示和交互改动：写入 `frontend/开发日志.md`。
- 后端通信、设备接入、告警、控制和数据库改动：写入本文件的“更新记录”，同时更新 `.project-memory.md`。
- 新增或调整操作规则时：必须说明影响哪一项需求、为什么改、如何验证。

---

## 更新记录

| 时间 | 内容 |
|---|---|
| 2026-07-24 10:52 | 完成阶段 C 第 3 项真实 API 接入边界。新增服务器环境变量配置、只读 HTTP 客户端、连接/读取超时、最多 5 次失败重试限制和安全日志规则；真实环境只允许 HTTPS，本机联调可使用 localhost。新增 `GET /api/v1/dashboard/integration/dji/readiness` 检查配置是否完整，但不返回地址、客户端编号或密钥。当前默认关闭，不会连接真实设备或发送控制指令；后端 11 条测试、前端生产构建和打包后运行时接口检查均通过。 |
| 2026-07-24 09:08 | 完成阶段 C 的数据可靠性提示。网页每秒根据统一快照的 `updatedAt` 与 `staleAfterSeconds` 判断数据是否过期；设备 `OFFLINE` 单独提示；版本不支持、缺少来源/时间/关键对象、无效过期时限或错误时间格式会显示为格式异常。数据过期或格式异常时，返航和告警确认入口会禁用，避免用不可信状态作控制判断；收到下一份有效快照后自动恢复。前端生产构建通过。 |
| 2026-07-24 08:57 | 完成阶段 C 的统一实时数据格式。`GET /api/v1/dashboard/status` 与 WebSocket 统一返回版本化快照，包含数据来源、UTC 更新时间、过期判断阈值、任务、设备、告警和返航指令状态；模拟器已先按该格式输出，后续 DJI Cloud API 只能在后端转换后再提供给网页。当前 `returnCommand` 只是模拟返航的状态回执，不开启真实控制。字段定义见 `docs/统一实时数据格式.md`；后端自动化测试和前端生产构建均通过。 |
| 2026-07-23 16:49 | 完成同事交接资料整理。新增 `docs/同事交接说明.md`，交接包仅包含源码、测试、建表脚本、文档和环境变量示例；明确排除本机 H2 数据库、日志、依赖缓存、构建产物、临时文件、补丁和任何真实凭证。交接后同事可独立启动本机演示，不会共用或覆盖当前电脑的数据。 |
| 2026-07-23 16:43 | 完成基础稳定性修复。后端重启后会恢复最近一条告警确认记录，避免历史显示已确认、实时页却重新显示待确认；确认记录先成功写入数据库，再更新内存状态，数据库失败时不会误报已确认。新增 4 条自动化测试，覆盖演示权限、共享环境禁用演示控制、告警恢复和写入失败。共享 MySQL 配置默认禁用演示身份控制，未接入真实登录前不能执行控制；前端改为同域接口和代理连接，便于后续部署。 |
| 2026-07-23 16:23 | 完成统一接口错误与加载提示。新增 `ApiErrorHandler`，将业务拒绝、非法参数和无法读取的请求体统一返回 `{ status, message }`；前端返航、告警确认和历史查询共用请求处理，断网、无权限、请求内容错误、状态冲突与服务异常会显示中文原因。已验证未登录返航返回 `403` 中文提示，非法筛选和错误请求体返回 `400` 中文提示；前端构建和历史页浏览器验收通过。 |
| 2026-07-23 16:12 | 完成统一操作历史。后端新增 `GET /api/v1/dashboard/history`，将 H2/MySQL 中的返航记录与告警确认记录按时间倒序合并，并支持 `type=ALL/RETURN/ALERT`、`result=ALL/成功/失败` 筛选；非法筛选返回 `400`。新增操作历史服务与稳定记录编号，前端可查看记录详情。已完成后端测试、接口筛选和浏览器验收；返航成功、返航失败、告警确认均能查询。为本机临时 `5174` 前端端口补充 CORS 放行，生产部署仍应只配置实际站点域名。 |
| 2026-07-23 15:58 | 完成返航二次确认。飞行操作员或管理员第一次点击返航时，只会打开确认窗口，核对操作人、设备、任务进度、电量和演示安全条件；只有第二次点击“确认发起返航”才会调用后端安全检查接口。已完成前端构建和浏览器验收，确认弹窗展示期间没有产生新的返航操作记录。 |
| 2026-07-23 15:51 | 完成阶段 A 的演示登录与角色权限。前端新增飞行操作员、管理员、只读访客和未登录状态；只读或未登录时禁用返航与告警确认入口并显示原因。后端新增身份请求头校验，未登录和只读访客直接返回 `403`，不再相信前端提交的操作人姓名。已验证未登录 `403`、只读访客 `403`、飞行操作员可发起请求并正确写入操作人。当前为演示身份，正式环境需要接入真实认证与授权。 |
| 2026-07-23 15:26 | 完成共享 MySQL 的代码准备与告警确认持久化：新增 MySQL 驱动、Flyway 自动建表脚本及 `mysql` 配置文件；返航记录和告警确认记录均可写入 H2 或 MySQL。新增 `GET /api/v1/dashboard/alerts/history` 告警历史接口。已在本机 H2 实测告警确认写入、返航记录保留、重启后两类记录均可查询；尚未连接真实 MySQL，待提供云数据库连接信息后联调。 |
| 2026-07-23 15:11 | 完成返航操作记录持久化：后端接入 Spring Data JPA 与 H2 文件数据库，新增操作记录实体、仓库和服务。返航成功或失败都会写入数据库，服务启动时会恢复最近一次操作，`GET /api/v1/dashboard/operations` 可查询完整历史。已实测写入一条低电量拒绝记录并重启服务，重启后仍可查询。当前仅持久化返航记录，告警确认记录仍待后续接入；正式部署应切换至 MySQL。 |
| 2026-07-23 14:45 | 完成返航演示控制与安全检查：新增 `POST /api/v1/dashboard/return` 和 `GET /api/v1/dashboard/operations`。后端会检查操作人、设备在线、电量、天气、任务状态和重复请求，并记录成功或失败的返航操作；网页新增返航按钮、四种演示场景、返航进度与最近操作记录。已验证低电量拒绝、正常返航完成和前端构建。当前操作记录仅保存在内存中，重启服务后会清空。 |
| 2026-07-23 11:28 | 修正模拟任务完成后的状态：完成后无人机高度为 0 m、设备转为在线待命，任务管理页与指挥中心共用实时进度和状态。模拟 REST 接口统一使用 `/api/v1/dashboard`；已完成浏览器连续更新验收。 |
| 2026-07-23 10:54 | 完成告警确认闭环：新增事件管理页面与后端确认接口。处理人、处理时间和处理结果由后端保存，并通过 WebSocket 同步到指挥中心；已确认告警不再计入待确认数量。此改动落实“设备状态检测与告警（P0）”的处理留痕规则。当前记录仅在服务运行期间保存，接入 MySQL 后需改为持久化历史记录。已用浏览器完成一次填写处理结果、确认、页面实时更新和接口数据核对。 |
| 2026-07-22 | 根据项目需求整理九项功能、P0/P1/P2 优先级、操作规则、验收标准和现有差距。 |
