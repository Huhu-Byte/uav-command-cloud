# 前端开发日志

> 用于记录每次前端的重要调整。出现问题时，先按日期查看当时改动过哪些页面和文件，便于快速定位和单独修改。

## 记录规范

- 每完成一个前端功能或重要调整，就新增一条记录。
- 每条记录写明时间、目的、改动文件、实际改动和验证结果。
- 如果后来修复问题，也要写明问题表现和修复方式。

---

## 2026-07-24 13:31

**目的**：补齐任务和成果中心的核心页面内交互，完成前端第一轮可操作页面。

**改动文件**：`src/App.vue`、`src/styles.css`、`开发日志.md`。

**完成内容**：

- 任务管理支持填写任务名称、设备、执行时间、频率和关联路线；提交后新增到当前浏览器会话的列表，并自动打开详情。
- 成果中心支持按类型和状态筛选，支持预览演示流程，并可导出当前筛选结果为 CSV 清单。
- 所有新增弹窗都明确标注：任务尚未写入服务器，照片和视频尚未接入真实文件。

**验证结果**：前端生产构建通过；独立 Edge 验收窗口实测新建“南门围栏复检”任务后进入列表并显示详情，视频筛选将成果收敛为 1 条，预览弹窗显示正确，CSV 文件成功下载；浏览器控制台 0 错误、0 警告。

---

## 2026-07-24 11:32

**目的**：按新的开发顺序先补齐前端业务页面，航线规划暂缓。

**改动文件**：`src/App.vue`、`src/styles.css`、`开发日志.md`。

**完成内容**：

- 新增实时监控页面，包含主画面区域、通道切换、飞行参数和离线状态。
- 完成设备管理页面，包含设备汇总、实时列表、设备详情和维护提示。
- 新增成果中心，包含照片/视频成果列表、演示预览和任务关联信息。
- 新增系统管理页面，展示实时连接、DJI Cloud API 就绪状态、演示权限和安全边界。
- 航线规划保留入口并明确标注“暂缓”，不占用当前开发优先级。

**验证结果**：前端生产构建通过；Playwright 逐页点击验收实时监控、设备管理、成果中心、系统管理和航线暂缓页，实时数据与后端就绪接口正常，浏览器控制台 0 错误、0 警告。

---

## 2026-07-24 11:09

**目的**：确保网站可直接打开，并消除浏览器控制台中的资源错误。

**改动文件**：`index.html`、`public/favicon.svg`、`开发日志.md`。

**完成内容**：

- 增加站点图标声明和本地 SVG 图标，修复浏览器请求 `/favicon.ico` 时产生的 404。
- 启动后端 `8080` 与前端 `5173` 服务，并在 Edge 中打开指挥中心。

**验证结果**：前端生产构建通过；Playwright 实测页面标题、导航、实时数据和任务状态正常，浏览器控制台 0 错误、0 警告。

---

## 2026-07-23 16:49

**目的**：为同事提供可直接启动的完整源码交接包，避免携带本机依赖缓存、日志和数据。

**改动文件**：`README.md`、`.env.example`、`docs/同事交接说明.md`

**完成内容**：

- 补充前端启动、代理连接和分开部署时的环境变量说明。
- 交接包保留前端源码与依赖清单，排除 `node_modules`、`dist` 和运行日志。

**验证结果**：交接说明包含前端构建命令；交接压缩包内容会在生成后逐项核对。

---

## 2026-07-23 16:43

**目的**：让前端从本机演示平稳过渡到服务器部署，并避免多个本地入口造成混乱。

**改动文件**：`src/App.vue`、`vite.config.js`、`.env.example`、`README.md`

**完成内容**：

- 前端接口和实时连接改为默认请求当前网站域名；开发环境由 Vite 自动转发到本机后端。
- 增加可选环境变量示例，前后端分开部署时无需改页面代码。
- 清理重复开发服务，仅保留 `http://127.0.0.1:5173` 一个前端入口。

**验证结果**：前端生产构建通过；浏览器实测指挥中心显示“实时数据已连接”，操作历史可通过新代理正常读取。

---

## 2026-07-23 16:23

**目的**：让返航、告警确认和操作历史在服务不可用或请求失败时，给出能直接理解的原因，而不是技术 JSON。

**改动文件**：`src/App.vue`、`src/styles.css`

**完成内容**：

- 三类后端请求统一通过同一调用方法处理，断网时会提示检查后端是否启动。
- 无权限、请求内容错误、状态冲突和服务异常会显示对应中文原因。
- 返航和告警确认的成功、失败提示采用不同颜色，并补充屏幕阅读器提示；按钮在提交期间继续显示“正在处理”。
- 操作历史保留“正在读取”与失败提示，不再将后端返回的 JSON 原文直接展示给使用者。

**验证结果**：前端生产构建通过；浏览器实测操作历史正常加载 4 条记录。后端接口实测未登录返航返回 `403` 中文原因，非法筛选和错误请求体返回 `400` 中文原因。

---

## 2026-07-23 16:12

**目的**：把返航与告警确认的留痕集中到一个页面，方便操作人员按类型和结果回查。

**改动文件**：`src/App.vue`、`src/styles.css`

**完成内容**：

- 顶栏新增“操作历史”入口，普通查看不受演示身份限制。
- 页面提供记录类型、处理结果筛选和手动刷新；可查看返航请求与告警确认的统一时间线。
- 选中任一记录后显示操作人、时间、关联设备及安全检查或告警处理详情。
- 补齐窄屏布局，并修复本机 `5174` 临时前端端口访问后端时被浏览器拦截的问题。

**验证结果**：前端生产构建通过；浏览器实测显示 4 条历史记录，切换“告警确认”后仅显示对应记录和完整详情。

---

## 2026-07-23 15:58

**目的**：让返航这一高风险演示操作在发送后端请求前，先由操作人再次核对关键状态，避免误触。

**改动文件**：`src/App.vue`、`src/styles.css`

**完成内容**：

- “立即返航”首次点击改为打开确认窗口，不直接发送请求。
- 窗口展示操作人、操作设备、当前任务进度、剩余电量和当前演示安全检查说明。
- 只有点击“确认发起返航”才调用后端；取消或点击遮罩不会发送返航请求。

**验证结果**：前端生产构建通过；浏览器实测确认窗口展示完整信息，弹窗期间返航状态和最近操作记录均未变化。

---

## 2026-07-23 15:51

**目的**：为返航和告警确认增加清晰的演示身份与前端权限提示，避免只读访客误以为自己可以执行控制操作。

**改动文件**：`src/App.vue`、`src/styles.css`

**完成内容**：

- 顶栏新增演示身份选择：飞行操作员、管理员、只读访客和未登录。
- 只读访客或未登录时，返航按钮、返航场景选择和告警确认表单会被限制，并明确提示需要切换的身份。
- 请求后端时自动传递当前演示身份，不再在页面中写死操作人姓名。

**验证结果**：前端生产构建通过；浏览器切换为“访客演示 · 只读访客”后，确认返航按钮与场景选择均禁用，并显示权限提示。

---

## 2026-07-23 14:45

**目的**：在指挥中心提供可演示的安全返航操作，让操作人员能看到返航进度、拒绝原因和最近一次操作留痕。

**改动文件**：`src/App.vue`、`src/styles.css`

**完成内容**：

- 新增“立即返航”按钮，并提供正常返航、低电量、设备离线和高风险天气四种演示场景。
- 点击后由网页请求后端安全检查；页面会展示返航中、已返航或被拒绝的明确结果。
- 新增返航状态卡、返航进度和最近操作记录；任务管理页会同步显示“返航中”或“已中止”。

**验证结果**：前端生产构建通过；浏览器已确认正常返航完成后会显示“无人机已安全返航”、冻结的任务进度、`0 m` 高度和最近操作记录。

---

## 2026-07-23 11:28

**目的**：让任务管理页与指挥中心使用同一份实时任务状态，避免同一任务在两个页面显示不同进度。

**改动文件**：`src/App.vue`

**完成内容**：

- 当前巡检任务的状态和完成进度改为读取后端实时状态。
- 任务完成后，任务列表自动从“执行中”切换为“已完成”。

**验证结果**：浏览器连续观察到任务管理页进度从 `89%` 到 `95%` 再到 `100%`，并自动切换为“已完成”。

---

## 2026-07-23 10:54

**目的**：让告警从指挥中心的提示信息变成可查看、可填写处理结果、可确认并可追溯的事件。

**改动文件**：`src/App.vue`、`src/styles.css`

**完成内容**：

- 新增“事件管理”页面，展示告警设备、等级、发生时间、原因和当前处理状态。
- 待确认告警必须填写处理结果；提交后页面展示处理人、处理时间和处理结果。
- 指挥中心根据后端返回的确认状态更新待确认数量和风险提示，不再把已确认告警算作待处理。

**验证结果**：浏览器实测填写“已确认现场情况，继续观察风速变化。”并确认后，事件页面显示“已确认”和完整处理记录；指挥中心待确认告警由 `01` 变为 `00`。

---

## 2026-07-23 09:29

**目的**：修正模拟实时数据的业务逻辑，使飞行中的电量、进度与任务状态符合现实规律。
**改动文件**：`src/App.vue`、`../backend/src/main/java/com/uavcommand/realtime/DroneStatusService.java`、`AGENTS.md`
**完成内容**：
- 飞行中的任务进度只会向前推进，电量只会下降；不再使用循环重置数值。
- 进度达到 100% 时，页面会显示“巡检任务已完成”，无人机 02 会切换为“在线待命”。
- 在 `AGENTS.md` 增加业务数据真实性规则，要求后续实时数据验收必须比较连续更新周期。
**验证结果**：已运行 `npm run build` 与 `mvn test`，均通过；浏览器连续观察到进度 73% → 78%、电量 68% → 63%、里程 18.4 km → 18.6 km。

---

## 2026-07-22 14:11

**目的**：建立 Vue 3 前端骨架和指挥大屏的第一版演示页面。

**改动文件**：`src/App.vue`、`src/main.js`、`src/styles.css`、`package.json`。

**完成内容**：左侧菜单切换；指挥大屏的设备、任务、事件概览；窄屏适配。

---

## 2026-07-22 15:00

**目的**：开始实现可操作的任务管理页面，并建立独立前端日志。

**改动文件**：`src/App.vue`、`src/styles.css`、`开发日志.md`。

**完成内容**：

- 新增任务状态筛选：全部、执行中、待执行、已完成、已暂停。
- 新增演示任务列表，展示任务名称、执行设备、计划时间、状态和进度。
- 点击任一任务可在右侧查看任务路线、负责人和当前进度。
- 保留原有指挥大屏，并补齐任务管理页面的窄屏显示。

**验证结果**：已运行 `npm run build`，构建通过。

---

## 2026-07-24 14:31

**目的**：精简网页版导航，改为桌面端左侧侧边栏，同时保留窄屏可用性。

**改动文件**：`src/App.vue`、`src/styles.css`、`开发日志.md`。

**完成内容**：

- 桌面端使用左侧常驻侧边栏，集中显示全部页面入口、实时连接状态和演示身份。
- 主工作区增加当前模块与实时更新时间；原有页面、弹窗和演示交互均保留。
- 宽度不超过 `900px` 时自动切换为顶部横向滚动导航；更窄屏幕进一步收紧品牌与导航文字。

**验证结果**：`npm run build` 通过；Playwright 只读点击“实时监控”和“任务管理”均正常切换，页面保持“实时数据已连接”。未涉及 App、航线规划、真实设备或 DJI 项目操作。

---

## 2026-07-24 15:07

**目的**：让网页新建任务在关闭或刷新网页后仍可保留。

**改动文件**：`src/App.vue`、后端任务接口与数据库迁移、`开发日志.md`。

**完成内容**：

- 任务页启动和进入页面时读取后端任务列表；保存任务时将名称、设备、时间、频率和路线提交给后端。
- 新建任务仅允许飞行操作员或管理员，后端默认写入“待执行”和 `0%` 进度。
- 页面明确提示：当前操作只保存任务资料，不会下发航线、飞行或设备控制指令。

**验证结果**：后端 `mvn test` 的 13 条测试通过，前端 `npm run build` 通过；本机后端重启后，浏览器重新打开任务页仍能读到验收任务。未涉及 App、航线规划、真实设备或 DJI 项目操作。

---

## 2026-07-24 15:19

**目的**：让成果中心的清单不再只存在于前端演示数据中。

**改动文件**：`src/App.vue`、后端成果接口与数据库迁移、`开发日志.md`。

**完成内容**：

- 成果中心改为从后端读取照片、视频成果清单，原有筛选、预览占位和 CSV 导出保留。
- 现阶段只保存成果资料、关联任务、设备、采集时间、位置和状态；不上传、播放或下载真实媒体文件。

**验证结果**：后端 `mvn test` 的 13 条测试通过，前端 `npm run build` 通过。未涉及 App、航线规划、真实设备或 DJI 项目操作。

---

## 2026-07-24 15:36

**目的**：补齐待执行任务的资料修改能力。

**完成内容**：仅飞行操作员或管理员可编辑待执行任务；执行中和已完成任务无法修改。编辑只更新任务资料，不发送航线或设备控制指令。

**验证结果**：后端 `mvn test` 的 13 条测试通过，前端 `npm run build` 通过。

---

## 2026-07-24 16:18

**目的**：为同事接手当前网页成果提供准确的启动、验收与安全边界说明。

**完成内容**：更新 `docs/同事交接说明.md`，同步当前页面范围、任务/成果持久化、测试数量、无中文路径启动要求、交接包范围和真实设备限制。

---

## 2026-07-24 15:40

**目的**：让任务与成果页面明确显示后端数据正在读取或读取失败。

**完成内容**：新增加载中、失败提示和手动刷新按钮；读取失败不会再显示在任务编辑表单内。

**验证结果**：前端 `npm run build` 通过。未涉及 App、航线规划、真实设备或 DJI 项目操作。

---

## 2026-07-24 15:45

**目的**：按任务快速定位巡检成果。

**完成内容**：成果中心新增关联任务筛选，可与类型、状态筛选组合使用，并影响 CSV 导出范围。

**验证结果**：前端 `npm run build` 通过。

---

## 2026-07-24 16:12

**目的**：整理当前阶段的前端交付边界与后续验证顺序。

**完成内容**：新增阶段复盘文档，明确后续优先稳固现有网页数据闭环与页面级验收；不规划 App、不提前开发航线，真实设备只在明确同意后做只读验证。

**验证结果**：后端 `mvn test` 的 13 条测试通过，前端 `npm run build` 通过。

---

## 2026-07-22 15:05

**目的**：参考 Xiaomi MiMo 官网的简洁信息层级，重新设计整个前端的页面骨架与指挥中心。

**改动文件**：`src/App.vue`、`src/styles.css`、`设计参考记录.md`、`开发日志.md`。

**完成内容**：

- 以居中顶栏替换左侧菜单，减少长期占用的深色区域。
- 用“飞行正在进行”大标题和实时航线舞台，明确页面唯一焦点。
- 将设备、里程、点位和事件压缩为下方数据带，减少卡片堆叠。
- 同步重做任务管理页，使其与指挥中心使用同一套导航、留白和排版。
- 新增 `设计参考记录.md`，说明外部设计参考的来源和实际借鉴范围。

**验证结果**：已运行 `npm run build`，构建通过。

---

## 2026-07-22 14:49

**目的**：将设计原则落实到前端样式，建立可复用的基础视觉规范。

**改动文件**：`src/styles.css`、`开发日志.md`。

**完成内容**：

- 新增颜色、文字、边框、圆角和阴影等 CSS 设计变量。
- 统一指挥大屏的卡片、标题、数据带和移动端分隔线。
- 用更轻的阴影与统一品牌色替换零散样式，任务卡增加低对比度渐变。
- 为风险处理按钮补充悬停反馈，保持界面简洁但有操作感。

**验证结果**：已运行 `npm run build`，构建通过。

---

## 2026-07-22 14:36

**目的**：让指挥大屏更有辨识度，同时降低信息密度，呈现更简洁的实时飞行工作台。

**改动文件**：`src/styles.css`、`开发日志.md`。

**完成内容**：

- 将深色横幅改为轻量的任务标题区，减少大面积色块。
- 将四项关键数据改为一条连续数据带，突出数值、弱化边框。
- 将模拟地图作为唯一主要视觉区域，加入更简洁的地形层次。
- 统一卡片圆角、间距和按钮尺寸，降低页面的“管理后台”感。

**验证结果**：已运行 `npm run build`，构建通过。

---

## 2026-07-22 15:10

**目的**：重新设计指挥大屏，使它更贴近无人机巡检任务的实时指挥场景。

**改动文件**：`src/App.vue`、`src/styles.css`、`开发日志.md`。

**完成内容**：

- 将原来的普通卡片布局改为“今日巡检指挥中心”布局，并增加系统状态和当前时间。
- 新增在线设备、执行任务、巡检里程、待处理事件四项关键数据。
- 新增模拟飞行地图、航线、无人机位置、飞行参数和当前任务进度。
- 新增无人机机队状态、最新活动时间线和风险提醒区域。
- 保留任务管理入口和设备、事件管理跳转，补齐窄屏显示。

**验证结果**：已运行 `npm run build`，构建通过。
| 2026-07-24 08:57 | 阶段 C：前端改为接收统一实时快照。页面会把后端的任务、设备、告警和返航指令状态转换为现有指挥中心字段，并在顶栏显示数据来源和最近更新时间；页面不直接依赖模拟器或未来 DJI Cloud API 的原始字段。前端生产构建通过。 |

---

## 2026-07-24 09:08

**目的**：在数据不可信时明确提醒，并阻止用户基于过期或格式异常的数据发起控制。

**改动文件**：`src/App.vue`、`src/styles.css`、`开发日志.md`。

**完成内容**：

- 每秒依据数据更新时间和过期时限判断实时数据是否过期。
- 单独提示离线设备；实时格式错误、数据过期时显示原因并禁用控制入口。

**验证结果**：已运行 `npm run build`，构建通过。
