# 留空时：开发环境由 Vite 转发到本机后端；部署环境请求同一网站域名。
# 如前后端必须分开部署，再设置完整地址，例如：https://api.example.com/api/v1/dashboard
VITE_API_BASE_URL=

# 如 WebSocket 不与前端同域，再设置完整地址，例如：wss://api.example.com/ws/drone-status
VITE_WS_URL=
