> 自动维护的关键信息，用于上下文衔接与跨对话记忆。
> 每次完成一项任务后，请在此文件尾部追加最新进展。

---

## 项目概况

- **项目名称**：上云api私有化部署
- **项目路径**：`C:\Users\x\Desktop\workplace\上云api私有化部署`
- **项目目标**：大疆上云API无人机巡检系统的私有化部署（网页版，不做AI和APP）
- **项目范围**：
  - 后端（Java Spring Boot）— 处理业务逻辑、与大疆通信、管理数据
  - 前端（Vue 3 网页）— 指挥大屏、航线规划、任务管理、设备管理、事件管理、巡检报告、系统管理
  - 数据库（MySQL）— 存储所有数据
  - 部署配置 — Docker Compose + nginx
  - AI 识别（暂缓）
  - 手机 APP（暂缓）
  - 单兵设备端 APP（暂缓）
- **技术栈**：Spring Boot / Vue 3 / MySQL / DJI Cloud API
- **对标系统**：GitHub unzhangruiqing/dji-cloud-api-drone-inspection3（16★，源码需联系微信获取）
- **参与者**：新手，需要耐心解释、一步步来

---

## 项目参与者

- **角色**：新手 / 逐步学习中
- **沟通偏好**：
  - 需要耐心解释，不要默认我有技术背景
  - 每个概念都讲清楚再往下走
  - 不要一上来就让我做选择题（比如"选 A 还是 B"）
  - 先解释清楚选项是什么、有什么区别，再让我决定

---

## 已完成任务

_（按时间倒序排列）_

| # | 日期 | 任务描述 | 关键产出 |
|---|------|---------|---------|
| 41 | 2026-07-24 13:31 | 完成前端任务与成果核心交互 | 支持任务新建和详情、成果筛选预览与 CSV 导出；前端第一轮页面完成 |
| 40 | 2026-07-24 11:32 | 补齐前端业务页面并暂缓航线规划 | 新增实时监控、设备管理、成果中心和系统管理；浏览器逐页验收无错误 |
| 39 | 2026-07-24 11:09 | 启动网站并完成浏览器验收 | 前后端服务正常运行；补齐站点图标；Edge 页面与实时数据正常，控制台无错误 |
| 38 | 2026-07-24 10:52 | 完成真实 API 凭证、超时、重试与日志边界 | 凭证仅由服务器环境变量注入；只读 HTTP 客户端应用超时和有限重试；就绪接口不泄露敏感配置 |
| 37 | 2026-07-24 09:08 | 完成数据过期、离线与格式异常提示 | 页面每秒判断时效；不可信数据时禁用控制，离线设备单独提示 |
| 36 | 2026-07-24 08:57 | 完成阶段 C 统一实时数据格式 | REST 与 WebSocket 改为版本化快照；包含来源、更新时间、任务、设备、告警和返航指令状态 |
| 35 | 2026-07-23 16:49 | 完成同事交接包整理 | 新增交接说明并生成不含本机数据、依赖缓存和敏感文件的源码压缩包 |
| 34 | 2026-07-23 16:43 | 完成盘查问题修复与基础稳定化 | 告警确认重启恢复；新增 4 条自动化测试；前端接口可配置；共享环境默认禁用演示控制 |
| 33 | 2026-07-23 16:23 | 完成统一接口错误与加载提示 | 后端统一中文错误格式；返航、告警确认和历史查询显示易懂原因与加载状态 |
| 32 | 2026-07-23 16:12 | 完成统一操作历史页面 | 返航与告警确认记录统一查询、按类型/结果筛选及详情查看；后端重启后仍可查询 |
| 31 | 2026-07-23 11:28 | 修正模拟任务完成状态并同步任务页面 | 任务完成后高度为 0 m；任务管理页实时显示后端进度和状态；接口统一使用 `/api/v1/` |
| 30 | 2026-07-23 10:54 | 完成事件管理与告警确认闭环 | 新增事件管理页面和确认接口；处理人、时间、结果由后端保存并实时同步到指挥中心 |
| 29 | 2026-07-23 10:06 | 调整 AI 协作与记录规则 | 普通开发步骤允许自主确认；关键节点主动同步；所有实际改动和验收结果必须写入日志 |
| 28 | 2026-07-23 10:06 | 完成模拟告警实时链路并修正任务完成后的数据变化 | 后端生成风速阈值告警并经 WebSocket 推送到指挥中心；任务完成后进度、电量和里程保持稳定 |
| 16 | 2026-07-22 14:11 | 明确记忆读取与记录流程 | 新会话必须先读取记忆；每项完成后记录关键节点与完成时间 |
| 17 | 2026-07-22 15:00 | 实现前端任务管理页面并创建前端开发日志 | 增加任务筛选、任务列表与详情面板；新增 `frontend/开发日志.md` |
| 18 | 2026-07-22 15:10 | 重新设计指挥大屏 UI | 增加任务指挥中心、模拟飞行地图、机队状态、活动记录和风险提醒 |
| 19 | 2026-07-22 14:36 | 精简指挥大屏视觉设计 | 收敛色彩与卡片样式，将地图和实时任务作为界面焦点 |
| 20 | 2026-07-22 14:49 | 建立前端基础设计变量 | 统一指挥大屏的颜色、圆角、阴影和关键组件样式 |
| 21 | 2026-07-22 15:05 | 参考 MiMo 官网重构前端整体设计 | 顶栏替换侧栏；以实时飞行主舞台重做指挥中心；新增设计参考记录 |
| 22 | 2026-07-22 15:12 | 安装 Playwright（Codex）skill | 可在后续会话中打开网页、操作页面并进行界面截图验证 |
| 23 | 2026-07-22 15:25 | 整理九项业务需求与操作规范 | 新增 `docs/业务与操作规范.md`，明确 P0/P1/P2、验收标准和当前差距 |
| 24 | 2026-07-22 16:49 | 建立模拟实时数据后端与前端实时接收能力 | 新增 Spring Boot / WebSocket 后端骨架；指挥大屏日期、时间和关键飞行数据可自动刷新 |
| 25 | 2026-07-22 17:13 | 安装本地 Java 环境并验证实时后端 | 安装 Java 17 与 Maven 3.9.9；后端编译成功并已在 8080 端口返回模拟实时状态 |
| 26 | 2026-07-23 09:17 | 浏览器验收模拟实时链路 | Edge 页面保持实时连接，进度、电量和里程在 3 秒推送周期后均已变化 |
| 27 | 2026-07-23 09:29 | 修正模拟状态连续性与验收规则 | 飞行中电量不再回升；完成任务会切换为已完成状态；`AGENTS.md` 增加业务真实性规则 |
| 15 | 2026-07-22 | 安装 UI/UX Pro Max（Codex） | 全局安装 `ui-ux-pro-max-cli`，并在项目 `.codex/skills/` 生成 Codex UI/UX 技能 |
| 14 | 2026-07-22 | 优化指挥大屏布局 | 重排设备、任务、事件区块；统一卡片样式并适配窄屏 |
| 13 | 2026-07-22 | 添加指挥大屏事件概览 | 展示一条高优先级待处理事件及其发现时间 |
| 12 | 2026-07-22 | 添加指挥大屏任务概览 | 展示一条演示巡检任务及其执行设备、完成进度 |
| 11 | 2026-07-22 | 添加指挥大屏设备概览 | 展示三台演示无人机的名称、状态和电量 |
| 10 | 2026-07-22 | 实现前端菜单切换 | 左侧菜单可切换模块标题与说明，并高亮当前选项 |
| 9 | 2026-07-22 | 初始化 Vue 前端项目 | 安装 Vue、Vite；创建基础指挥大屏与启动配置 |
| 8 | 2026-07-22 | 项目范围确认与总结 | 确定只做网页版，去掉AI和APP；更新全部文档 |
| 7 | 2026-07-22 | 搭建项目文件框架 | 创建 backend/frontend/deploy/scripts 目录结构，添加 .gitignore 和 README |
| 6 | 2026-07-22 | 创建 AGENTS.md | AI行为说明书，借鉴B站高收藏视频经验，三层约束结构，新手友好 |
| 5 | 2026-07-22 | 重写开发规范文档 | 根据新手反馈重写，每个概念都用通俗语言逐一解释 |
| 4 | 2026-07-22 | 创建开发规范文档（初版） | 初版写得太简略，后重写 |
| 3 | 2026-07-22 | 调研 B 站视频对标系统 | 定位到 GitHub 仓库 dji-cloud-api-drone-inspection3，梳理完整功能架构与技术栈 |
| 2 | 2026-07-22 | 项目迁移到桌面 workplace | 从 Documents 迁移至桌面 `workplace\上云api私有化部署` |
| 1 | 2026-07-22 | 创建 .project-memory.md 项目记忆文件 | 初始化记忆文件模板，完成首次 Git 提交 (f497e9a) |

---

## 待办事项 / 下一步

1. **项目范围已确定** — 只做网页版（网页控制台），不做 AI 和 App；未经使用者明确同意，不提前规划或开发 App
2. **开发路径已固化** — 后续任务按 `docs/开发路径与迭代计划.md` 的 A 至 E 阶段推进；以大疆司空 2 作为界面与长期产品方向参考，但不脱离当前功能主线；云部署属于可延后执行的阶段 B
3. **当前下一步** — 前端第一轮页面和核心交互已完成，侧边栏布局、任务与成果清单持久化已完成；航线规划继续暂缓。先稳固现有网页数据闭环与页面级验收；真实设备仍只做经过同意的只读验证。详见 `docs/当前阶段复盘与后续安排.md`
<!-- 最后更新：2026-07-29 | 已按 DJI 官方 Cloud API 文档校正架构：云端或现场网线均通过 EMQX；不经司空 2 转发 -->
+<!-- 最后更新：2026-07-29 11:55 | 完成全面浏览器验收，10页0错误0警告；当前环境稳定，准备下一阶段 -->

## 最近会话记录


- **2026-07-29**：使用者澄清有两条独立候选路线：（1）云端 MQTT：前端、后端和 EMQX 全部部署到云服务器，由 FlightHub 2 的「司空 Sync → MQTT-Bridge」转发数据；（2）现场网线直连：Dock 3 经现场局域网直接连接本地后端与前端，目标是不经过 FlightHub 2 的 MQTT-Bridge 和 EMQX。现有代码仅完成路线一的 MQTT 接收准备；路线二是否可行，取决于 DJI 是否提供可供第三方使用的 Dock 3 局域网数据接口、认证方式与消息格式，尚未验证。当前不部署、不连接设备，`UAV_DJI_MQTT_ENABLED=false` 持续生效；已同步更新 `docs/数据对接架构.md`。


- **2026-07-29**：已按 DJI 官方 Cloud API 文档完成架构校正。官方明确大疆机场、DJI Pilot 2 和网页均是 MQTT Client，连接中心化 Broker；设备向 `osd`、`state`、`status` 等 Topic 上报，后端订阅后再推送网页。因此有两种部署方式：（1）云端：EMQX、后端、前端均在云服务器；（2）现场网线：Dock 3 和部署 EMQX、后端、前端的服务器在同一局域网。两种方式都需要 EMQX 或兼容 MQTT 5.0 的 Broker；官方文档未提供绕过 Broker、通过网线直接读取 Dock 3 数据的第三方协议。此前将司空 2 转发和无 EMQX 网线直连混入本项目的表述已由 `docs/数据对接架构.md` 更正。当前不部署、不连接设备，`UAV_DJI_MQTT_ENABLED=false` 持续生效。

- **2026-07-29**：使用者提供并要求按 DJI 官方“Pilot 上云”“机场上云”“Dock 3 设备属性”“Dock 3 设备管理”文档重新理解链路。结论：Pilot 上云是 Pilot 2 打开第三方 H5、经 JSBridge 校验 License 并加载上云模块后，Pilot 2 作为 MQTT Client 连接 Broker；机场上云则由 Pilot 2 填写 Broker 地址和账号密码，随后 Dock 3 自己直接连接 Broker。Dock 3 必须依次请求配置与 License、完成组织绑定、上报 `update_topo` 并获得 `status_reply`，随后才持续上报 `osd`（0.5 Hz）和 `state`。现有项目只具备订阅实时 Topic 的准备，尚缺 Pilot H5/JSBridge、MQTT 参数接口、Dock 请求应答、拓扑应答、EMQX/TLS 和测试机场验证，因此不能连接真实设备。现场网线方案仍需 EMQX；它是 Dock 3 连接局域网 Broker，而非绕过 Broker 直连后端。已完整重写 `docs/数据对接架构.md`。

- **2026-07-29**：补读 DJI 官方“机场上云 Topic 定义”和“Pilot 上云 Topic 定义”。两套 Topic 共用 `osd`、`state`、`services`、`events`、`requests`、`status`、`property/set`、`drc` 等基础消息外壳，但对应不同设备域：Pilot 上云面向遥控器及其连接设备，机场上云面向 Dock 3 网关及其子设备飞行器。服务端必须通过 `gateway_sn` 和机场 `update_topo` 识别设备类型后再解析，不能因 Topic 名相同而混用属性模型；下行操作 Topic 在当前阶段继续禁用。已更新 `docs/数据对接架构.md`。

- **2026-07-29**：系统阅读 DJI 官方 Cloud API 中文文档库。官方仓库中文版共 112 篇：总览 9 篇、快速开始 4 篇、功能集 21 篇、调试 3 篇、接口参考 75 篇；另补读在线文档站的 Dock 3 MQTT 19 个页面。确认第三方平台部署可为私有化或公有云，只要网关设备能访问平台；基础服务包括 MQTT、HTTPS、WebSocket、对象存储、数据库和 Redis。Pilot 侧通过 H5/JSBridge 加载上云模块，机场侧由 Pilot 2 配置 Broker 后 Dock 3 直接连入。Pilot 与 Dock 共用基础 Topic 外壳但设备模型不同；Dock 3 全部配置、组织、拓扑应答、实时属性、任务、媒体、告警、日志、直播、固件与控制接口的文档范围已完成索引。当前项目继续限定为真实连接关闭、只读准备，未实现的上云握手和下行控制不得启用。

- **2026-07-29**：按官方 Pilot、机场与 Topic 时序审查现有 MQTT 代码。确认当前实现仅订阅 `osd`、`state`、`status`，没有 `requests_reply`、`status_reply` 或 Pilot H5/JSBridge；并且错误地把 App 凭证用于 MQTT 登录，部署配置还硬编码了未验证的 Broker 地址和设备编号。当前报文解析未读取官方 `data` 包装层，也没有按网关拓扑区分机场、飞行器与 Pilot。已将真实接入前的六项必改内容写入 `docs/数据对接架构.md`；未修改业务代码、未开启 MQTT、未连接设备或发布控制消息。

> **当前停点标记（2026-07-29）**：DJI Cloud API 官方文档、Pilot 上云、机场上云、Pilot/Dock Topic 与 Dock 3 在线接口已完成梳理；现有代码的真实接入必改项已明确并记录在 `docs/数据对接架构.md`。从此处继续时，先按“拆分 MQTT 凭证 → 机场上云应答 → 官方报文解析 → Pilot H5 → 部署安全”的顺序实施。当前保持 `UAV_DJI_MQTT_ENABLED=false`，不连接设备、不发送控制指令。本次仅完成文档与差距审查，未创建 Git 提交。

## 本次会话关键节点

- **2026-07-26 18:53**：继续当前阶段的稳定化验收。确认任务、成果和操作历史页面均已具备读取中、读取失败提示和手动刷新，不重复开发相同功能；同步修正 `AGENTS.md` 中“任务和成果未持久化”的过期描述。后端 `mvn test` 的 13 条测试和前端生产构建均通过。验收时本机 `5173`、`8080` 服务未运行，未擅自启动常驻服务，因此浏览器页内验收待服务启动后补做；未涉及航线规划、真实设备、DJI 项目操作或 App。
- **2026-07-24 16:18**：更新 `docs/同事交接说明.md`，同步当前 8 个网页模块、任务/成果持久化、13 条后端测试、真实设备限制、无中文路径启动注意事项、源码包范围和同事验收清单。交接文件明确不含本机数据和凭证，且不做 App、航线或真实控制。
- **2026-07-24 16:12**：完成当前阶段复盘与代码精简。确认网页已统一使用 `/history`，删除未被网页、测试使用的旧 `/operations`、`/alerts/history` 接口及其专用中转方法/查询；不删除任何历史数据表或记录。新增 `docs/当前阶段复盘与后续安排.md`，后续先稳固网页版数据闭环，再在使用者明确同意后做真实设备只读验证；不规划 App、不提前开发航线。后端 13 条测试和前端生产构建通过。
- **2026-07-24 15:45**：成果中心新增按关联任务筛选，可与类型、状态筛选组合使用，导出清单会遵循当前筛选结果。前端生产构建通过；未涉及 App、航线规划、真实设备或 DJI 项目操作。
- **2026-07-24 15:40**：完善任务与成果页面的数据读取反馈。两页新增加载中、读取失败提示和手动刷新按钮；失败信息显示在页面中，不再混入新建任务弹窗。前端生产构建通过。未涉及 App、航线规划、真实设备或 DJI 项目操作。
- **2026-07-24 15:36**：完成待执行任务编辑。网页详情弹窗只在“待执行”且具备操作身份时显示编辑入口；后端 `PUT /api/v1/dashboard/tasks/{id}` 也会校验身份和任务状态，执行中、已完成等任务会拒绝修改。编辑仅更新名称、设备、时间、频率和路线资料，不下发航线或设备控制。后端 13 条测试和前端生产构建通过；未涉及 App、真实设备或 DJI 项目操作。
- **2026-07-24 15:19**：完成成果清单持久化。新增成果表与 `GET /api/v1/dashboard/results`，将原有四条照片/视频演示记录迁入后端数据库；成果中心启动和进入页面时读取后端列表，筛选、预览占位和 CSV 导出继续保留。当前只保存清单资料与采集时间，不上传或播放真实媒体文件。后端 13 条测试和前端生产构建通过；未涉及 App、航线规划、真实设备或 DJI 项目操作。
- **2026-07-24 15:07**：完成网页任务持久化。新增任务表与 `GET/POST /api/v1/dashboard/tasks`：任务信息由后端保存，默认状态为“待执行”、进度为 `0`；仅飞行操作员或管理员可新建，只读访客只能查看。网页任务页改为读取后端任务列表，新建任务提交后调用保存接口；明确该操作只保存资料，不会下发航线、飞行或设备控制指令。新增 2 条后端测试，后端 13 条测试、前端构建均通过；重启本机后端后，重新打开网页仍能读取已保存的验收任务。验收中只新增本机演示数据库任务，不涉及 App、航线规划、真实设备或 DJI 项目操作。
- **2026-07-24 14:31**：完成网页端布局精简。桌面端改为左侧常驻侧边栏，集中放置全部页面入口、实时连接状态和演示身份；主工作区新增当前模块与实时更新时间。窄屏自动退回为顶部横向滚动导航，避免菜单挤压内容。前端生产构建通过；Playwright 只读验收确认“实时监控”和“任务管理”可从侧边栏正常切换，页面仍显示实时连接状态。未涉及 App、航线规划、真实设备或任何 DJI 项目操作。
- **2026-07-24 13:31**：完成前端任务与成果中心的核心交互。任务页新增新建表单，可填写名称、设备、时间、频率和关联路线；提交后新增到当前浏览器会话的列表并自动打开详情，明确尚未写入后端。成果中心新增类型/状态筛选、预览弹窗和 CSV 导出；导出按当前筛选结果生成清单。修复热更新时默认任务表单初始化顺序导致的浏览器错误，改为直接初始化表单对象。前端生产构建通过；独立 Edge 验收窗口成功新建“南门围栏复检”、验证详情、视频筛选、预览和 CSV 下载，浏览器控制台 0 错误、0 警告。前端第一轮业务页面与核心交互已完成，航线规划继续暂缓。
- **2026-07-24 11:32**：按使用者最新安排调整开发顺序：先完成网页端业务页面，航线规划暂缓。新增实时监控、设备管理、成果中心和系统管理页面；实时监控展示主画面占位、设备通道、飞行参数与离线状态，设备管理使用实时数据展示列表与详情，成果中心提供照片/视频成果结构，系统管理读取后端 DJI 就绪接口并展示权限和安全边界。航线规划入口明确显示“暂缓”。前端生产构建通过，Playwright 逐页点击验收全部新增页面，浏览器控制台 0 错误、0 警告；服务保持运行并已将 Edge 切回指挥中心。下一项继续完善这些页面内部的可操作交互，不提前开发航线规划。
- **2026-07-24 11:09**：已启动后端 `http://127.0.0.1:8080` 与前端 `http://127.0.0.1:5173`，并在 Edge 中打开网站。状态接口返回 `schemaVersion: 1.0`、数据来源为 `SIMULATOR`，网页显示“实时数据已连接”。浏览器验收时发现唯一错误为缺少站点图标，已新增 `frontend/public/favicon.svg` 并在 `index.html` 声明；前端生产构建通过，重新打开页面后控制台为 0 错误、0 警告。当前两个服务保持运行，用户可直接访问网页。
- **2026-07-24 10:52**：完成阶段 C 第 3 项真实 API 接入边界。新增 `DjiCloudApiProperties` 与 `DjiCloudApiClient`，真实地址、客户端编号和密钥只能由服务器环境变量注入；默认 `UAV_DJI_CLOUD_ENABLED=false`，不会连接真实设备。只读 HTTP 客户端已真正应用连接超时和读取超时，并按配置执行最多 5 次重试；请求只允许相对路径，真实地址要求 HTTPS（本机 localhost 联调除外）。错误日志只记录固定操作类型、次数和异常类别，不记录 URL、凭证或异常正文。新增 `GET /api/v1/dashboard/integration/dji/readiness`，只返回启用/就绪状态与超时重试参数，不返回敏感值。新增 6 条专项测试；后端现有 11 条测试全部通过，前端生产构建通过，并用打包后的后端实测就绪接口返回默认关闭状态且不含敏感字段。下一项为阶段 C 第 4 项真实测试设备只读验证，当前仍不发送任何真实控制。
- **2026-07-24 09:08**：完成阶段 C 第 2 项数据可靠性提示。前端每秒按统一快照的 `updatedAt` 和 `staleAfterSeconds` 判断是否过期；`OFFLINE` 设备会单独提示；版本不支持、缺少来源/更新时间/关键对象、无效过期时限和错误时间格式均会显示为格式异常。数据过期或格式异常时，返航和告警确认控制入口会自动禁用，直到收到下一份有效快照；不会自动发出控制指令。前端生产构建通过，已更新 `docs/统一实时数据格式.md` 和业务规范。下一项为阶段 C 第 3 项：为未来真实 API 准备服务器环境变量、超时、重试和错误日志边界，仍不连接真实设备。
- **2026-07-24 08:57**：完成阶段 C 第 1 项统一实时数据格式。新增 `RealtimeStatusSnapshot`，`GET /api/v1/dashboard/status` 和 WebSocket `/ws/drone-status` 现统一返回 `schemaVersion: 1.0` 的版本化快照，含 `source`、`updatedAt`、`staleAfterSeconds`、任务、设备、告警和返航指令状态；模拟器先在后端转换，未来 DJI Cloud API 也必须转换为同一格式后才能给网页。前端保留当前界面但在单一转换处读取新格式，并显示“模拟飞行状态”和最近更新时间。后端 5 条自动化测试、前端生产构建和状态接口实测通过；本机 Playwright 启动 Edge 后异常关闭，未能产出浏览器截图，需在可用浏览器环境补一次可视化验收。新增 `docs/统一实时数据格式.md` 并同步更新后端说明、业务规范和前端开发日志。下一项为阶段 C 第 2 项：页面提示数据过期、离线和格式错误。
- **2026-07-23 16:49**：整理同事交接包。新增 `docs/同事交接说明.md`，说明项目现状、包含与排除内容、启动方式、验证命令和真实设备限制；交接压缩包将保留源码、测试、文档和环境变量示例，排除本机 H2 数据、依赖缓存、构建产物、日志、临时文件、补丁和 Git 历史，避免泄露本机数据或增大传输体积。未创建 Git 提交。
- **2026-07-23 16:43**：完成盘查后的基础稳定化。修复告警确认记录与重启后实时状态不一致：服务启动时恢复最近确认记录，且只有数据库写入成功后才更新内存状态。新增 4 条后端自动化测试，覆盖演示权限、共享环境禁用演示控制、告警恢复和写入失败；`mvn test` 全部通过。前端接口与 WebSocket 改为同域访问，开发环境由 Vite 代理转发到本机后端，新增 `.env.example`；已清理重复服务，仅保留 `http://127.0.0.1:5173`，浏览器实测实时连接和操作历史均正常。MySQL 配置默认关闭演示身份控制，真实登录尚未接入时不会允许控制操作。未创建 Git 提交；下一项仍为阶段 C 的统一数据格式与来源标识。
- **2026-07-23 16:23**：完成阶段 A 的统一接口错误与加载提示，阶段 A 五项任务全部完成。后端新增 `ApiErrorHandler`，统一将业务拒绝、非法参数和错误请求体返回为包含状态码与中文原因的固定结构；前端返航、告警确认和历史查询改用同一请求处理方法，断网、无权限、参数错误、状态冲突和服务异常均显示易懂提示，成功与失败颜色明确，提交期间保留加载状态。后端测试、前端构建通过；接口实测未登录返航为 `403` 中文原因，非法筛选和错误请求体为 `400` 中文原因；浏览器历史页正常加载 4 条记录。云部署继续暂缓，下一项转入阶段 C 的统一数据格式与来源标识准备。
- **2026-07-23 16:12**：完成阶段 A 的统一操作历史。新增 `GET /api/v1/dashboard/history`，后端从持久化的返航记录和告警确认记录生成统一时间线，按时间倒序返回，并支持类型（全部/返航/告警）和结果（全部/成功/失败）筛选；非法条件返回 `400`。前端新增“操作历史”页面，包含筛选、刷新、列表和详情；只读访客也可查看。后端测试和前端构建通过；浏览器实测显示 4 条记录，切换“告警确认”后只显示对应记录及完整详情。验收中发现 `5174` 临时前端端口未在后端 CORS 放行列表，已补充本机 `5174`，不改变生产部署的域名配置原则。下一项为统一接口错误提示与加载状态优化。
- **2026-07-23 15:58**：完成返航二次确认。飞行操作员或管理员首次点击“立即返航”后只会打开确认窗口，窗口展示操作人、无人机 02、实时任务进度、电量和当前安全检查说明；再次点击“确认发起返航”才会调用既有后端接口。已完成前端构建和浏览器验收，确认窗口展示期间没有产生新的返航操作记录。下一项为统一操作历史页面。
- **2026-07-23 15:51**：完成阶段 A 的演示登录与角色权限。前端提供飞行操作员、管理员、只读访客和未登录四种演示身份；只读和未登录时禁用返航、返航场景和告警确认入口。后端新增 `X-Demo-User` 与 `X-Demo-Role` 校验，拒绝前端伪造操作人：未登录与只读访客请求均返回 `403`，飞行操作员请求可成功进入安全检查并以请求头身份记录。后端测试、前端构建和浏览器权限界面验收均通过。当前仅为演示身份，正式部署必须接入真实认证与授权。
- **2026-07-23 15:37**：读取大疆司空 2 官方页面，确认其定位涵盖远程控制、智能任务规划、多类型航线管理与开放集成。已将其写入开发路径文档的“对标原则”：界面信息层级可随相关功能逐步对齐，但当前不单独开启界面重做；仍优先完成阶段 A 的登录、权限、二次确认和操作历史。
- **2026-07-23 15:32**：新增 `docs/开发路径与迭代计划.md`，把项目目标、当前基线、A 至 E 阶段、每次开发的验收门槛、范围控制规则和下一项任务写成统一依据。已明确云端共享部署可暂时跳过，不阻塞本机安全闭环开发；下一项按阶段 A 实现演示登录与角色权限。`AGENTS.md` 已加入文档索引。
- **2026-07-23 15:26**：完成共享 MySQL 的代码准备和告警确认记录持久化。新增 MySQL 驱动、Flyway 自动建表脚本、`mysql` 运行配置、告警记录实体/仓库/服务，以及 `GET /api/v1/dashboard/alerts/history` 历史接口。返航和告警确认均可写入本机 H2 或未来的 MySQL；已在 H2 实测张晨确认告警后记录写入，重启后告警历史与返航历史均仍可查询。真实 MySQL 尚未连接，待准备阿里云 RDS 后用环境变量联调；数据库账号密码不会写入 Git。
- **2026-07-23 15:11**：完成返航操作记录持久化。后端新增 Spring Data JPA、H2 文件数据库、操作记录实体和仓库；返航成功或失败都会写入本机 `C:\Users\x\.uav-command` 下的数据库文件，服务启动时会恢复最近一次记录，操作历史接口会从数据库读取。已实际写入一条低电量拒绝记录，重启后端后仍可查询到同一条记录。当前只持久化返航记录；告警确认记录仍在内存中，正式部署需将 H2 切换为 MySQL。
- **2026-07-23 14:45**：完成返航演示控制与安全检查。新增 `POST /api/v1/dashboard/return` 返航请求和 `GET /api/v1/dashboard/operations` 操作记录接口；后端在返航前检查操作人、设备在线、电量、高风险天气、任务状态和重复请求，并将成功或失败原因写入内存操作记录后通过 WebSocket 同步。正常返航时任务进度会冻结，无人机高度逐步降到 `0 m`，结束后显示“已返航”；前端新增返航按钮、演示场景、返航进度和最近操作记录。已验证低电量拒绝、正常返航完成、后端测试和前端构建通过。操作记录暂未接入数据库，服务重启后会清空。
- **2026-07-23 11:28**：修正任务完成状态并完成前后页面同步。模拟任务完成后，无人机高度从 80 m 变为 0 m，设备状态为在线待命；任务管理页直接使用实时状态更新当前任务的进度和状态。接口统一调整为 `/api/v1/dashboard`。后端构建与前端构建通过；浏览器连续观察到任务管理页 89% → 95% → 100%，最终指挥中心显示 100%、0 m、41% 电量和 19.3 km。已将 `.codex`、`.superpowers` 和浏览器临时目录加入 Git 忽略规则；已创建本地 Git 提交 `2dc8c2d`（`feat: 完成实时告警与任务状态闭环`）。
- **2026-07-23 10:54**：完成事件管理与告警确认闭环。新增后端 `POST /api/dashboard/alerts/current/acknowledge`，要求填写处理结果，保存处理人、处理时间和结果后立即通过 WebSocket 推送。前端新增事件管理页面；确认后指挥中心的待确认告警从 01 变为 00。后端构建与前端构建均通过；浏览器实测确认“已确认现场情况，继续观察风速变化。”后，事件页和接口均显示处理人张晨、时间 2026-07-23 10:47 和相同处理结果。
- **2026-07-23 10:06**：完成第二条 P0 实时链路。后端会在模拟风速接近阈值时生成“一般”告警，通过现有 WebSocket 状态消息推送；指挥中心实时显示待确认告警数量、标题、说明和首次发生时间。修复了任务完成后电量仍下降、里程仍增加的问题，完成后数据固定为进度 100%、电量 41%、里程 19.3 km。后端构建、前端构建和浏览器连续两个更新周期验收通过；控制台仅有已知的 `favicon.ico` 404。用户允许常规开发步骤由 AI 自主确认，要求关键节点继续告知，所有改动必须说明并写入日志；该规则已同步到 `AGENTS.md`。
- **2026-07-23 09:29**：修正了模拟状态使用取余重置而导致电量回升的问题。飞行中任务的进度只增加、电量只下降；进度达到 100% 后页面会显示任务已完成、无人机切换为在线待命。已在 `AGENTS.md` 写入业务数据真实性规则。前端构建和后端测试均通过；浏览器连续观察到进度 73% → 78%、电量 68% → 63%、里程 18.4 km → 18.6 km。
- **2026-07-23 09:17**：已用本机 Edge 完成浏览器验收：网页显示“实时数据已连接”，在一个 3 秒推送周期后，任务进度从 89% 变为 75%，无人机 02 电量从 61% 变为 76%，今日巡检里程从 26.8 km 变为 27.2 km。控制台仅有 `favicon.ico` 缺失导致的 404，不影响实时功能。
- **2026-07-22 14:11**：已完成 Vue 前端骨架、菜单切换和指挥大屏基础概览（设备、任务、事件）；已完成布局优化；已为 Codex 安装 UI/UX Pro Max；确认后续每一步操作前先说明计划并等待用户确认。
- **2026-07-22 15:00**：完成任务管理第一版：可按状态筛选演示任务，选择任务后可查看详细信息与执行进度；已创建 `frontend/开发日志.md`，后续每次重要前端改动会写入该文件。
- **2026-07-22 15:10**：完成指挥大屏重新设计，改为实时任务指挥中心布局，含模拟飞行区域、数据总览、机队状态、活动记录和风险提醒。
- **2026-07-22 14:36**：根据“稍微有新意且简洁”的反馈，进一步精简指挥大屏的视觉层级，突出实时任务和模拟飞行地图。
- **2026-07-22 14:49**：将设计规范落地为 CSS 变量，统一指挥大屏的基础视觉语言，便于后续单独调整颜色、圆角和阴影。
- **2026-07-22 15:05**：读取 Xiaomi MiMo 官网后，借鉴其大留白、顶栏导航和核心内容聚焦的通用方法，完成前端整体骨架和指挥中心重构。
- **2026-07-22 15:12**：从 OpenAI 官方 GitHub skills 仓库安装 `playwright` skill；该 skill 将在下一轮会话可用，用于网页与界面验证。
- **2026-07-22 15:25**：已将九项业务需求写入 `docs/业务与操作规范.md`；当前前端仅有演示原型，P0 的实时监控、在线运维、告警、设备控制和上云 API 均未开发，后续应先搭建模拟实时通信链路。
- **2026-07-22 16:49**：已创建 Spring Boot 后端骨架：每 3 秒生成一份模拟无人机状态，经 WebSocket `/ws/drone-status` 推送；同时提供 `/api/dashboard/status` 便于查看当前状态。前端会自动连接该服务，连接前保留演示数据；指挥中心的日期和时间每 30 秒刷新，因此跨天无需刷新网页。前端生产构建已通过；本机未检测到 Java 和 Maven，尚不能启动后端验证。
- **2026-07-22 17:13**：已在 `C:\Users\x\.tools` 安装并验证 Java 17（Temurin 17.0.19）和 Maven 3.9.9；后端 `mvn test` 构建成功。因 Windows 对原项目中文路径的启动限制，新增只作入口用的 `C:\Users\x\uav-backend-run` 快捷目录，指向原 `backend`，不移动原项目。后端已成功响应 `http://127.0.0.1:8080/api/dashboard/status`；前端 `http://127.0.0.1:5173/` 也可访问。

---

## 关键决策与架构记录

### 决策记录

| 日期 | 决策 | 说明 |
|------|------|------|
| 2026-07-22 | 不做AI识别 | 专注网页版核心功能，AI后续再补 |
| 2026-07-22 | 不做手机APP | 专注PC网页版，APP后续再补 |
| 2026-07-22 | 不做单兵设备端APP | 同上 |
| 2026-07-22 | 不购买对标系统源码 | 选择自研，后续如需可联系（微信wsdj1566zhc） |

### 对标系统架构参考

| 模块 | 说明 |
|------|------|
| uavs_server | 后台主程序（Spring Boot） |
| uavs_web | Web 前端（Vue 3） |
| dji_api | 大疆机场接入服务 |
| uav_platform.sql | 数据库脚本（MySQL） |

### 本项目当前文件结构

```
上云api私有化部署/
├── backend/       -- 后端（待写代码）
├── frontend/      -- 前端（待写代码）
├── deploy/        -- 部署配置（待写）
├── scripts/       -- 工具脚本（待写）
├── docs/          -- 文档
│   ├── 开发规范文档.md
│   └── 业务与操作规范.md
├── AGENTS.md      -- AI行为说明书
├── .project-memory.md
└── .gitignore
```

## 技术要点

- Java：Temurin 17.0.19，位置 `C:\Users\x\.tools\jdk-17.0.19+10`
- Maven：3.9.9，位置 `C:\Users\x\.tools\maven-full\apache-maven-3.9.9`
- 后端端口：`8080`；REST 检查接口：`GET /api/v1/dashboard/status`；实时推送：`ws://localhost:8080/ws/drone-status`
- 前端开发服务：`http://127.0.0.1:5173/`

---

## 已知问题 / 注意事项

_（当前存在的坑、待修复的 Bug、待确认的假设）_

- Flyway 启动时会提示本机 H2 `2.2.224` 版本高于其已测试版本 `2.2.220`；本机自动建表、写入和重启查询均已通过，当前不影响功能。正式环境将使用 MySQL。

---

## 参考资源

- B站视频（主）：[大疆上云api智能巡检](https://www.bilibili.com/video/BV1u8XaBqEN5) — UP主「专注无人机」
- GitHub 仓库：https://github.com/unzhangruiqing/dji-cloud-api-drone-inspection3 （16★）
- GitHub 仓库（旧版）：https://github.com/unzhangruiqing/dji-cloud-api-drone-inspection-master （9★）
- 联系方式：微信 wsdj1566zhc（完整源码需联系获取）
- UP主其他相关视频：YOLOV8识别（2035播放）、飞控系统（861播放）、航线规划（362播放）

---

<!-- 最后更新：2026-07-28 16:38 | 成果中心已限定本机工作空间并明确媒体来源；真实 DJI 连接持续关闭 -->

## 最近会话记录


- **2026-07-29 全天**：完成 DJI Dock 3 真实设备接入的全链路调研与代码准备。

  调研成果：从 `2026H1实操训练营-机场3+司空final.pptx` 中明确了 DJI Dock 3 的数据对接架构——Dock 3 通过 FlightHub 2（司空2）的「司空 Sync → MQTT-Bridge」功能把设备遥测推送到第三方自建的 EMQX 服务器，而非直接调用 DJI Cloud API MQTT。发现 `api.dji.com` 在公网 DNS 中不存在（Google/Cloudflare/AliDNS 均返回 NXDOMAIN），但 DJI FlightHub OpenAPI 地址 `es-flight-api-cn.djigate.com` 可公网解析和访问。

  凭证已入：D:\UAV-Config\uav-command.env 中保存了完整的 DJI Cloud API 凭证（App ID: 191594，App Key，App License）+ Dock 3 SN（8UUXN2B00AO0ST）+ FlightHub JWT 令牌（org: 196282d8-e9d1-4f15-84c3-c7e997f13f57）。

  代码已完成：新增 `DjiMqttConfiguration.java`（MQTT 订阅 Bean，订阅 `thing/product/{sn}/osd` / `state` 和 `sys/product/{sn}/status`），`DjiCloudApiClient` 的 `Readiness` 记录新增 `MqttStatus` 字段，`RealtimeStatusPublisher` 新增 `publish(snapshot)` 方法（收到 DJI 数据自动停模拟器）。新增 `spring-integration-mqtt` 和 `spring-boot-starter-data-redis` 依赖。后端 `mvn test` 共 32 条全部通过，前端 `npm run build` 通过，容器重建后 readiness 端点返回 `mqtt.enabled` 状态。

  当前阻塞：（1）MQTT Broker 未部署——DJI 需要推送数据到自建 EMQX，本机无公网 IP，提出方案 A（Moquette + ngrok 内网穿透）和方案 D（阿里云 ECS）；（2）Docker Hub 网络不通，EMQX/Redis 镜像未能拉取。

  新增文档：`docs/数据对接架构.md`（含完整链路图、凭证清单、代码结构、阻塞点与解决方案）。

- **2026-07-29 14:10**：完成 DJI 真实设备接入前置基础设施补齐。新增 `DjiMqttProperties` 配置类（MQTT 连接参数：brokerUrl、clientId、超时、心跳、cleanSession），`DjiCloudApiClient` 的 `Readiness` 记录新增 `MqttStatus` 字段（enabled/brokerUrl），所有就绪检查分支返回空 MQTT 状态。新增 `spring-boot-starter-integration`、`spring-integration-mqtt`、`spring-boot-starter-data-redis` 三项 Maven 依赖，`application.yml` 新增 MQTT 和 Redis 占位变量，`compose.yaml` 预留 EMQX（emqx/emqx:5.7，MQTT 1883/WebSocket 8083/Dashboard 18083）和 Redis（7-alpine）两个 DJI profile 注释服务。`UavCommandApplication` 增加 `@EnableConfigurationProperties` 显式注册 `DjiCloudApiProperties` 和 `DjiMqttProperties`。后端 `mvn test` 共 32 条全部通过，前后端构建均通过，容器重建后 readiness 端点返回 `"mqtt":{"enabled":false,"brokerUrl":null}`。因 Docker Hub 网络不通，EMQX 和 Redis 镜像未能拉取，compose 中已注释保留，后续网络恢复后执行 `docker compose --profile dji up -d emqx redis`。未连接真实设备，`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false`、`UAV_DJI_MQTT_ENABLED=false` 持续生效。

- **2026-07-29 11:15**：完成全站浏览器逐页验收。已确认本机 Docker 三容器（MySQL、后端、Nginx 前端）运行正常，后端 `/api/v1/dashboard/status` 返回 v1.0 快照（模拟器/2 在线/1 离线），`/api/v1/dashboard/workspace` 返回 1 草稿/0 标注/4 成果/真实设备未连接。Playwright 在 Edge 中逐页打开：指挥中心、实时监控、任务管理（3 条任务）、设备管理（3 台）、事件管理（1 条告警/待确认）、成果中心（4 项/来源标识）、操作历史（空记录）、系统管理（工作空间/安全边界/DJI 关闭）、地图标注（空列表/新建表单）和航线规划（1 草稿/3 航点/预览/WPML 检查/前置资料/版本区）。10 页全部正常，浏览器控制台为 0 错误、0 警告。未修改业务代码，未接入真实设备，`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。当前环境与代码基均已稳定，下一步按规划应准备真实设备只读验证前置条件。
- **2026-07-27 11:15**：按 DJI 上云 API 官方环境准备清单完成本机只读盘点。当前 Windows 11、Java 17、Node.js 24、现有前后端服务均可用于继续开发；但未安装 Docker、MySQL、EMQX（MQTT 消息服务）、Redis 或 Nginx，且当前后端真实 DJI 接入开关仍为关闭状态。因此本机尚不满足官方完整上云 API 示例的运行环境，不能直接连接真实设备。后续需先确认设备型号、设备与服务器的网络连通方案，并获得只读连接的明确同意后，再选择安装容器化环境或准备独立服务器；不在当前电脑上直接尝试真实连接。
- **2026-07-27 09:13**：补齐现有网页数据闭环的后端自动化保护检查。新增成果清单读取顺序检查，以及操作历史合并与按类型筛选检查；这些检查用于防止后续调整时破坏已验收的数据展示，不改变任何业务接口、权限或设备控制。后端 `mvn test` 共 15 条测试全部通过。未接入真实设备、未操作 DJI 项目、未开发航线规划或 App。
- **2026-07-27 08:57**：完成现有网页数据闭环的浏览器验收。已启动本机后端（`8080`）和前端（`5173`），后端正常连接本机 H2 演示数据库，前端显示“实时数据已连接”。在 Edge 中逐页打开“任务管理”“成果中心”“操作历史”，三页均成功读取后端保存的数据；手动点击各页刷新后列表保持正常，浏览器控制台为 0 错误、0 警告。未修改业务代码，未接入真实设备、未操作 DJI 项目、未开发航线规划或 App。服务目前仍在本机运行，便于继续查看。
- **2026-07-27 12:00**：完成容器化运行环境的代码准备。新增 MySQL、Spring Boot 后端和 Nginx 前端三项服务的 Docker Compose 配置，以及前后端容器构建文件、Nginx API/WebSocket 转发配置和不含真实密码的环境变量模板。默认关闭 DJI Cloud API，MySQL 只在内部网络供后端使用，浏览器只访问 Nginx。已完成文件存在性和差异格式检查。尝试通过官方 `winget` 安装 Docker Desktop 两次，均在下载官方安装包时因 Windows 网络错误 `0x80072eff` 失败；Docker 尚未安装，因此本次无法进行容器实际构建和启动验收。待网络恢复后重试安装，再执行 `docker compose --env-file deploy/.env -f deploy/compose.yaml up --build -d`。
- **2026-07-27 12:56**：已在浏览器打开 DJI 上云 API 官方“基于 Docker 的部署”页面，并使用页面给出的阿里云 OSS 官方链接成功下载 Docker 示例包。文件保存为 `D:\UAV-Downloads\cloud_api_sample_docker.zip`，大小 `1,760,891,785` 字节（约 1.63 GB）。该包包含 DJI 官方示例源码和镜像，不是 Docker Desktop 安装程序；本次未解压、未运行其中镜像，也未填写或使用任何 DJI 凭证。
- **2026-07-27 13:00**：使用者明确要求：后续下载、安装和运行环境优先放在 `D:`，避免默认占用 `C:`。已确认 `D:` 可用且剩余空间约 141 GB。经使用者确认，DJI 官方 ZIP 已从 `C:\Users\x\Downloads` 移动至 `D:\UAV-Downloads`，文件大小已核对一致。
- **2026-07-27 13:15**：Docker Desktop 已安装至 `D:\Docker\Desktop`，并已成功启动 Docker 引擎（客户端与服务端版本均为 `29.6.2`）；Linux 容器运行数据已初始化到 `D:\Docker\wsl-data`。项目本机数据库密码配置已创建在未纳入 Git 的 `D:\UAV-Config\uav-command.env`。启动项目容器时，Docker Desktop 两次无法连接 Docker Hub 的 `registry-1.docker.io:443`，因此 `mysql:8.4` 镜像未能下载，MySQL、后端和 Nginx 尚未启动。已仅查看 DJI 官方 ZIP 的目录，确认其包含预打包镜像 `cloud_api_sample_docker_v1.10.0.tar`；未解压、未载入或运行任何 DJI 示例镜像。
- **2026-07-27 13:50**：完成容器化本机环境验收。Docker Desktop 已配置使用本机 VPN 代理后成功拉取所需基础镜像；由于容器内 Maven 访问中央仓库会收到 `403`，部署 Dockerfile 调整为仅打包本机已验证的后端 JAR 和前端 `dist`，不在容器内下载 JavaScript 或 Java 依赖。本机 `mvn package -DskipTests` 与 `npm run build` 均通过。`mysql`、`backend`、`web` 三个容器已启动，MySQL 健康检查通过，Flyway 已完成 4 条建表迁移；网页和经 Nginx 转发的状态接口均返回 `HTTP 200`，Edge 页面显示“实时数据已连接”，浏览器控制台为 0 错误、0 警告。访问地址：`http://127.0.0.1:8088`。真实 DJI 连接继续保持关闭。
- **2026-07-27 13:52**：已将 `D:\Docker\Desktop\resources\bin` 加入当前 Windows 用户的 PATH；新打开终端后可直接使用 `docker` 命令，无需填写完整 D 盘路径。
- **2026-07-27 14:02**：完成容器化 MySQL 环境的关键业务页面验收。浏览器依次打开“任务管理”“成果中心”“操作历史”，任务页正常显示 3 条任务及详情，成果页正常显示 4 条后端保存的成果记录和预览占位说明；操作历史查询正常，因该 MySQL 数据库首次创建且尚未执行新的演示操作，当前显示空记录属于预期结果。三个页面保持“实时数据已连接”，浏览器控制台为 0 错误、0 警告。未连接真实 DJI 设备，未发送任何真实控制指令。
- **2026-07-27**：完成首次本机 MySQL 数据备份演练。使用 Docker 容器内的 `mysqldump` 将 `uav_command` 数据库导出到 `D:\UAV-Backups`；备份文件大小为 `15,364` 字节，确认不是空文件。备份过程只读取数据库，不停止服务、不修改业务数据。
- **2026-07-27**：已逐页核对 DJI 上云 API 官方“环境准备清单”“基于源码的部署”“基于 Docker 的部署”和“Pilot 上云”说明。结论：源码部署与 Docker 部署是运行官方 Demo 的两种替代方式，不需要同时执行；Pilot 上云是服务端准备后由 DJI Pilot 2 通过 H5 登录页、License 校验和 MQTT 参数接入设备的流程。当前项目已具备 Java、Node、MySQL、Nginx 和 Docker 的本机模拟环境，但尚未具备官方真实接入所需的 EMQX（MQTT）、Redis、DJI 上云 API 应用凭证、Pilot 2 专用 H5/JSBridge 登录流程、遥控器可访问的 HTTPS 地址与测试设备。后续应先补齐真实设备只读接入前置条件，不能把官方旧 Demo 直接覆盖到当前项目。
- **2026-07-27**：已核对 DJI 官方调试页面“Pilot WebView 调试”“Pilot 日志导出”“MQTTX 调试工具”。WebView 可通过 Chrome DevTools 或仅测试环境启用 vConsole 排查登录页和 JSBridge；设备问题应保留发生时间、录屏/截屏、飞机/遥控器/Pilot 日志，Pilot 日志加密后需配套公钥与官方解密工具；MQTTX 是跨平台 MQTT 客户端，待部署 EMQX 和拿到测试账号后才用于只读订阅消息。当前本机仍只需使用浏览器、Docker 和后端日志，不安装或连接真实设备调试工具。

- **2026-07-28 08:42**：已安全接入桌面后端 V1.0 交接包中的航线草稿后端模块：新增草稿保存、读取、更新、删除、校验和 HTTP 自动化测试，以及 V5 数据库迁移。仅新增文件，未覆盖现有任务、成果、实时状态或容器配置。后端 `mvn test` 共 26 条测试全部通过，草稿接口的临时测试数据库已成功执行 V5 迁移；航线网页入口仍按既定安排暂缓，未连接真实设备。
- **2026-07-28 09:12**：完成航线规划网页与草稿后端接口联通。网页支持草稿读取、航点编辑、保存前校验、更新和删除；明确提示所有操作只管理草稿，不下发飞行指令。修正浏览器不能发送中文 HTTP 身份头的问题：页面继续显示中文姓名，接口改用英文身份代号。前端生产构建通过，Docker 本机 MySQL、后端、Nginx 已重建，V5 迁移已应用，浏览器可读取航线规划页面且控制台无页面脚本错误。当前容器配置按安全边界关闭演示身份写入，浏览器保存返回预期的 403 提示；未为验收而放开该保护，未连接真实设备。
- **2026-07-28 09:17**：经使用者同意，仅在本机模拟容器临时开启演示身份写入，完成航线草稿网页端到端验收：创建“东侧围栏验收草稿”后列表显示 1 条，修改名称后列表与编辑区同步，删除后列表回到 0 条；浏览器控制台为 0 错误、0 警告。验收草稿已删除。随后已删除 `D:\UAV-Config\uav-demo-validation.override.yaml` 临时覆盖文件，并重新确认容器环境为 `UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false`；未连接真实设备，未下发飞行指令。
- **2026-07-28 09:29**：完成“航线草稿 → 任务资料”安全闭环。任务新建和编辑表单改为从已保存航线草稿中选择路线名称；后端仍只保存名称副本，不建立草稿外键，因此以后修改或删除草稿不会影响已有任务。没有草稿时可明确选择“不关联航线草稿”；编辑旧任务时其原路线显示为“历史保留”，不会被清空。前端生产构建通过；容器网页验收确认两种情形均正常，浏览器控制台为 0 错误、0 警告。演示写入和真实 DJI 连接持续关闭。
- **2026-07-28 09:52**：完成航线草稿本地可视化预览。航线规划页会根据当前填写的有效坐标绘制航点连线相对示意，并实时计算有效航点数、预计航程、预计用时、平均高度和每个航点的动作摘要；页面明确标注这不是现场地图，不代表已下发路线。前端生产构建通过；浏览器验收默认 3 个航点显示为约 170 m、约 1 分钟、平均 80 m，动作摘要正确，控制台 0 错误、0 警告。演示写入和真实 DJI 连接持续关闭。
- **2026-07-28 10:41**：完成“任务选航线草稿摘要”浏览器验收。本机仅创建并保留一条“航线摘要验收草稿”（园区东侧、3 个航点、v1），未创建任务；在新建任务表单选择后，正确显示草稿名称、所属区域、自定义航点模式、3 个航点和 v1 版本。浏览器控制台为 0 错误、0 警告。验收结束后已使用基础 Compose 配置重建后端，实际环境确认 `UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false`，并移除 `D:\UAV-Config\uav-demo-validation.override.yaml` 临时覆盖文件；未连接真实设备、未下发航线或飞行指令。
- **2026-07-28 11:03**：完成工作空间最小基础层。新增 V6 数据库迁移，创建固定的“本机演示工作空间”，并让已有与后续航线草稿在数据库中归属该工作空间；新增只读接口 `GET /api/v1/dashboard/workspace`，返回工作空间名称、状态、草稿数量和真实设备连接开关。系统管理页新增工作空间状态卡。后端 `mvn test` 共 27 条测试通过，前端生产构建通过；容器重建后浏览器确认页面显示“本机演示工作空间”、1 条航线草稿和“真实设备未连接”，控制台 0 错误、0 警告。未新增工作空间写入入口，未连接 DJI Pilot 或真实设备，`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。
- **2026-07-28 13:04**：完成航线版本发布基础层。阅读 DJI 官方 WPML 概述后明确：真实可执行航线是包含 `template.kml`、`waylines.wpml` 和可选 `res` 的 `.kmz` 文件，且高度模式需要严格处理；当前不生成 `.kmz`。新增 V7 数据库迁移、航线版本快照实体和 `GET/POST /api/v1/route-drafts/{id}/versions` 接口。发布时会复制当时的名称、区域、模式、草稿版本和全部航点，后续修改草稿不改变已发布版本。航线规划页新增发布区和版本列表，并明确不会生成 DJI 文件、下发或执行航线。后端 `mvn test` 共 28 条测试通过，前端生产构建通过；容器页面验收显示版本区和“当前草稿尚未发布版本”，浏览器控制台 0 错误、0 警告。本机默认写入关闭，因此未点击发布、未新增版本数据；真实设备与 DJI 连接持续关闭。
- **2026-07-28 13:44**：完成 DJI WPML 生成前只读检查。根据 DJI 官方 `waylines.wpml` 规范，确认当前草稿已有航点经纬度、高度、速度和基础动作类型，但缺少且不能猜测：飞行器/负载机型、高度基准与换算依据、安全起飞与返航高度、结束及失联处置、偏航/转弯/云台规则。航线规划页新增检查面板，明确“当前不可生成”，不会生成、上传、下发或执行 DJI `.kmz`。前端 `npm run build` 通过；重建本机网页容器后，浏览器确认保留的“航线摘要验收草稿”仍正常显示，检查项完整可读，控制台 0 错误、0 警告。`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 保持关闭，未连接任何真实设备。
- **2026-07-28 14:14**：完成 DJI WPML 前置资料草稿保存。新增 V8 数据库迁移，在航线草稿与其发布版本快照中保存可选的设备机型、负载、高度基准说明、安全起飞/返航高度、结束及失联策略、偏航/转弯/云台规则；旧草稿可继续正常读取，所有资料仍可留空。航线页新增“生成前资料草稿”表单，未填资料时继续明确“当前不可生成”；选择“退出航线并执行失联动作”却未填具体动作时，后端会拒绝保存。后端 `mvn test` 共 29 条通过，前端 `npm run build` 通过；本机 MySQL 已成功应用 V8，浏览器控制台 0 错误、0 警告。未填写、保存或删除现有草稿资料，`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。
- **2026-07-28 15:48**：完成本地地图标注草稿基础层。新增 V9 数据库迁移、点/线/区域三种标注的保存、读取、更新、删除与坐标校验接口；所有标注归属本机演示工作空间，不连接 DJI Pilot，也不向真实地图或设备同步。网页新增“地图标注”页面，可查看空列表并填写本地标注；点、线、区域分别至少需要 1、2、3 个坐标点。后端 `mvn test` 共 32 条通过，前端 `npm run build` 通过；本机 MySQL 已成功应用 V9，浏览器控制台 0 错误、0 警告。未创建、修改或删除任何标注数据，`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。
- **2026-07-28 16:01**：完成工作空间地图标注数量联动。只读工作空间接口新增 `mapElementCount`，系统管理页的“本机演示工作空间”卡片现同时显示航线草稿 `1 条`、地图标注 `0 个` 和真实设备“未连接”。后端 `mvn test` 共 32 条通过，前端 `npm run build` 通过；容器网页验收和浏览器控制台均为 0 错误、0 警告。容器环境再次确认 `UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false`，未连接 DJI Pilot 或真实设备，未下发任何航线或飞行指令。
- **2026-07-28 16:20**：完成本机媒体资料与工作空间的归类联动。新增 V10 数据库迁移，把已有和后续成果清单归属到固定的本机演示工作空间；只读工作空间接口新增 `inspectionResultCount`，系统管理页现显示航线草稿 `1 条`、地图标注 `0 个`、媒体资料 `4 项`和真实设备“未连接”。后端 `mvn test` 共 32 条通过，前端 `npm run build` 通过，重新执行后端 `mvn package -DskipTests` 后重建容器，MySQL 已成功应用 V10；浏览器控制台为 0 错误、0 警告。此功能只归类已有清单，不上传、预览、下载或同步真实媒体文件；`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。
- **2026-07-28 16:38**：完成成果中心的本机资料来源与范围保护。新增 V11 数据库迁移，为成果清单保存 `LOCAL_CATALOG` 来源；成果读取改为只查询本机演示工作空间的数据，接口返回 `mediaSource`。网页新增明确边界提示、详情中的“资料来源”和“查看占位预览”；预览窗口明确不会读取真实媒体文件。后端 `mvn test` 共 32 条通过，前端 `npm run build` 通过，MySQL 已成功应用 V11；接口返回的 4 条记录均为 `LOCAL_CATALOG`，浏览器控制台为 0 错误、0 警告。未上传、下载、播放或同步真实媒体，`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。

+- **2026-07-29 11:15**：完成全站浏览器逐页验收。已确认本机 Docker 三容器（MySQL、后端、Nginx 前端）运行正常，后端 `/api/v1/dashboard/status` 返回 v1.0 快照（模拟器/2 在线/1 离线），`/api/v1/dashboard/workspace` 返回 1 草稿/0 标注/4 成果/真实设备未连接。Playwright 在 Edge 中逐页打开并截图：指挥中心、实时监控、任务管理（3 条任务）、设备管理（3 台）、事件管理（1 条告警/待确认）、成果中心（4 项/来源标识/筛选可选）、操作历史（空记录/筛选可选）、系统管理（工作空间/安全边界/DJI 关闭）、地图标注（空列表/新建表单）和航线规划（1 草稿/3 航点/预览图/WPML 检查/前置资料/版本区）。10 页全部正常，浏览器控制台为 0 错误、0 警告。未修改业务代码，未接入真实设备，`UAV_DEMO_AUTH_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。当前环境与代码基均已稳定，下一步按规划应准备真实设备只读验证前置条件。

- **2026-07-30 08:39**：完成真实 DJI 接入前的 MQTT 凭证边界修正。后端 MQTT 配置新增独立的 Broker 账号和密码，只能由 `UAV_DJI_MQTT_USERNAME`、`UAV_DJI_MQTT_PASSWORD` 注入；不再把 Cloud API 的 App ID 或 License 用作 MQTT 登录凭证，也移除了默认 Broker 地址。启用 MQTT 却缺少 Broker 地址、账号、密码或设备编号时，服务会在启动阶段拒绝连接。更新了后端与部署环境变量样例、数据对接架构文档，并新增单元测试。后端 `mvn test` 共 33 条通过；`UAV_DJI_MQTT_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 保持关闭，未连接设备或发布任何 MQTT 消息。下一步是实现机场上云所需的 `requests_reply` 与 `status_reply`，但在拥有测试机场、独立 EMQX 账号和使用者明确同意前不得启用连接。

- **2026-07-30 09:01**：依据 DJI 官方 Cloud API 公开文档核对机场上云的确切报文：`config` 回复直接提供 `app_id`、`app_key`、`app_license`、`ntp_server_host` 与端口；`airport_bind_status` 回复在 `data.output.bind_status` 中返回设备绑定状态；`update_topo` 回复在 `data.result` 中确认成功。新增 `DjiDockHandshakeService`，会保留请求的 `tid`、`bid` 与 `method`，离线构造上述三类回复；组织查询和绑定尚无可持久化的组织资料，因此不伪造成功回复。MQTT 新增 `UAV_DJI_MQTT_HANDSHAKE_ENABLED=false` 第二道开关：默认只准备回复、不发布。后端 `mvn test` 共 36 条通过，未连接设备、未启用 MQTT、未发送 MQTT 消息。下一步为组织绑定资料持久化，之后才可继续官方报文的 `data` 层遥测解析。

- **2026-07-30 10:44**：完成组织与设备“待绑定资料”基础层。新增 V12、V13 数据库迁移以及组织资料的读取、新建、修改、删除接口；网页在系统管理页中分开登记组织编号、组织名称、主控 SN、铭牌 SN、显示名称和可选机型标识，防止两个 SN 被混用。资料状态固定为“待绑定”，不保存设备绑定码、不向 DJI 设备发送绑定结果，也不声称已完成真实绑定。后端 `mvn test` 共 38 条通过，前端 `npm run build` 通过；`UAV_DJI_MQTT_ENABLED=false`、`UAV_DJI_MQTT_HANDSHAKE_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效。使用者提供的真实编号和绑定码未写入源码、Git 或项目日志。

- **2026-07-30 11:00**：完成 Dock 3 `osd` 遥测报文的离线解析保护。新增 `DjiDockTelemetryParser`，只从报文的 `data` 层读取经纬度、高度和电量；数值缺失、超出合理范围或仍处在报文根层时会拒绝生成网页快照，不再把读不到的字段错误显示为 `0`。当前只解析 Dock 网关自身的基础遥测，`data.devices` 子设备和按 `update_topo` 区分机场/飞行器的解析仍待真实测试资料核验后完成。网页把尚未确定的设备运行状态显示为“状态待解析”。后端 `mvn test` 共 40 条通过，前端 `npm run build` 通过；`UAV_DJI_MQTT_ENABLED=false`、`UAV_DJI_MQTT_HANDSHAKE_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效，未连接设备、未发布 MQTT 消息、未下发任何飞行指令。

- **2026-07-30 11:12**：完成 MQTT 网关编号边界澄清与敏感配置清理。运行配置从容易混淆的“设备编号”改为 `UAV_DJI_MQTT_GATEWAY_SN`，它只允许填写机场网关 SN，不能混用本机资料页中的主控 SN 或铭牌 SN；旧 `UAV_DJI_MQTT_DEVICE_SN` 仅在后端保留临时迁移兼容。部署配置中残留的真实机场编号已移除，改为只从未纳入 Git 的环境变量读取；未读取、复制或记录该编号。后端 `mvn test` 共 40 条通过，前端 `npm run build` 通过；`UAV_DJI_MQTT_ENABLED=false`、`UAV_DJI_MQTT_HANDSHAKE_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效，未连接设备、未发送 MQTT 或飞行控制消息。

- **2026-07-30 11:46**：依据 DJI 官方“教程地图”与官方 Java 示例仓库 `dji-sdk/DJI-Cloud-API-Demo` 的 `UpdateTopo`、`UpdateTopoSubDevice` 数据类，完成 `update_topo` 的本地安全摘要解析。新增 `DjiDockTopologyRegistry`，只在内存中记录机场网关和 `subDevices` 的 SN、域、类型、子类型、索引；`deviceSecret`、`nonce` 不保存、不写日志、不入库。MQTT 的 `status` 消息会先交给该解析器，再保留既有的握手回复准备逻辑；当前仍只将 Dock 网关自身的 `osd` 转为网页快照，尚未解析子设备属性。后端 `mvn test` 共 42 条通过；`UAV_DJI_MQTT_ENABLED=false`、`UAV_DJI_MQTT_HANDSHAKE_ENABLED=false`、`UAV_DJI_CLOUD_ENABLED=false` 持续生效，未连接设备、未发送 MQTT 或飞行控制消息。

- **2026-07-30 11:57**：完成待绑定资料与机场网关的明确对应。新增 V14 数据库迁移和可选字段“机场网关 SN”，网页、资料接口和数据库均可保存该字段；它与主控 SN、铭牌 SN 分开，只有明确填写后才可在未来作为 MQTT `gateway_sn` 的匹配依据。旧资料保持可读且可留空，系统仍固定显示“待绑定”，没有保存绑定码或报告真实绑定成功。后端 `mvn test` 共 42 条通过，前端 `npm run build` 通过；真实 MQTT、握手发布和设备连接持续关闭。

- **2026-07-30 12:11**：完成机场组织名称的只读回复准备。`airport_organization_get` 只有在请求 Topic 的网关 SN 与本机“待绑定”资料中明确填写的机场网关 SN 精确匹配时，才会准备包含组织名称的回复；无匹配资料时不伪造成功回复。此功能不读取或保存绑定码，不执行组织绑定，也不会在 MQTT 握手发布开关关闭时发送任何消息。后端 `mvn test` 共 42 条通过；真实 MQTT、握手发布和设备连接持续关闭。

- **2026-07-30 13:15**：完成机场组织绑定（airport_organization_bind）应答准备。DjiMqttProperties 新增 deviceBindingCode 字段（仅从 UAV_DJI_DEVICE_BINDING_CODE 环境变量读取，不入库、不写日志）；OrganizationDeviceProfileService 新增 matchesOrganizationGateway 校验方法；DjiDockHandshakeService 实现 airport_organization_bind：只有设备提交的 binding code 与服务器配置完全一致，且 organization_id 与 gateway SN 同时匹配已登记的"待绑定"资料时，才返回 result=0（成功），否则返回 result=1（失败）。配置为空或缺字段时不产生应答。后端 mvn test 共 44 条全部通过，UAV_DJI_MQTT_ENABLED=false、UAV_DJI_MQTT_HANDSHAKE_ENABLED=false、UAV_DJI_CLOUD_ENABLED=false 持续生效。至此 Dock 3 上云握手所需的四类应答（config / airport_bind_status / update_topo / airport_organization_get + airport_organization_bind）已全部完成准备。下一项为按官方报文解析 Dock 3 子设备（飞行器）遥测数据。

### 当前任务：解析 Dock 3 OSD 子设备（飞行器）遥测

**目标**：将 DjiDockTelemetryParser 从仅解析 Dock 网关自身遥测，扩展为同时解析 data.devices[] 中的子设备（飞行器）遥测。

**改动范围**：
- DjiDockTelemetryParser.java：注入 DjiDockTopologyRegistry，扩展 parseOsdSnapshot 解析子设备
- DjiDockTelemetryParserTest.java：新增子设备解析测试
- .project-memory.md：记录结果

**不改动**：前端、数据库迁移、MQTT 连接开关（持续关闭）。


- **2026-07-30 13:24**：完成 Dock 3 OSD 子设备（飞行器）遥测解析扩展。DjiDockTelemetryParser 注入 DjiDockTopologyRegistry，在解析 Dock 网关自身遥测后，同步解析 data.devices[] 中的子设备：按设备 SN 从拓扑注册表查找设备类型标注（飞行器/电池/子设备），并用与网关相同的遥测校验规则（坐标范围、高度范围、电量合理性）逐个验证。遥测不完整的子设备静默跳过，不伪造缺失数据。后端 mvn test 共 47 条全部通过，UAV 安全开关持续关闭。

### 当前任务：解析 Dock 3 state 消息中的设备运行状态

**目标**：DjiDockTelemetryParser 新增 parseState 方法，将 	hing/product/{sn}/state 报文中的运行状态（Dock 舱盖/急停/补光灯、飞行器飞行模式/起落架等）映射为网页可读的操作状态，替代当前的 "UNKNOWN"。

**改动范围**：
- DjiDockTelemetryParser.java：新增 parseState 方法 + 状态映射逻辑
- DjiMqttConfiguration.java：state Topic 路由到 parseState 方法
- DjiDockTelemetryParserTest.java：新增 state 解析测试
- .project-memory.md / 开发日志.md：记录


- **2026-07-30 13:43**：完成 Dock 3 state 消息解析。DjiDockTelemetryParser 新增 parseState 方法和内部 ConcurrentHashMap 设备状态表，解析 	hing/product/{sn}/state 报文中的运行状态：Dock 网关按 cover_state 和 emergency_stop_state 映射为"就绪"/"舱盖已打开"/"急停已触发"；飞行器按 flight_mode 映射为"手动操控"/"起飞中"/"降落中"/"航线飞行"/"悬停中"/"返航中"。parseOsdSnapshot 创建快照时从状态表查运行状态，无记录时仍为 UNKNOWN。DjiMqttConfiguration 已将 /state Topic 路由到 parseState。后端 mvn test 共 49 条全部通过（+2 条新测试），UAV 安全开关持续关闭。

### 当前任务：订阅并解析 Dock 3 events 消息（设备告警/事件）

**目标**：DjiMqttConfiguration 新增 events Topic 订阅，DjiDockTelemetryParser 新增 parseEvent 方法，将 DJI 设备事件（hms/电池/固件等告警）映射为统一的 AlertState 格式，合并到实时快照中。

**改动范围**：
- DjiMqttConfiguration.java：新增 events Topic 订阅 + 路由
- DjiDockTelemetryParser.java：新增 parseEvent 方法 + 事件→告警映射
- DjiDockTelemetryParserTest.java：新增测试
- .project-memory.md / 开发日志.md：记录


- **2026-07-30 14:15**：完成 Dock 3 events 消息订阅与解析。DjiMqttConfiguration 新增 	hing/product/{gatewaySn}/events Topic 订阅（QoS 1），DjiDockTelemetryParser 新增 parseEvent 方法：从事件报文中提取 method 映射为告警标题和级别（hms→健康告警、flight_task_ready→任务就绪等），生成统一 AlertState 存入 activeAlerts 列表（上限 20 条），每次 OSD 快照自动携带累积告警。后端 mvn test 共 50 条全部通过（+1 条事件测试），UAV 安全开关持续关闭。

### 当前任务：state/event 变化立即触发快照重推

**目标**：DjiDockTelemetryParser 缓存最近一次成功的 OSD payload，提供 refreshLatestSnapshot() 方法；DjiMqttConfiguration 在 state/event 处理后调用该方法，将更新后的状态/告警立即推送到前端。

**改动范围**：
- DjiDockTelemetryParser.java：缓存 lastOsdTopic/lastOsdPayload，新增 refreshLatestSnapshot()
- DjiMqttConfiguration.java：state/event 路由后调用 refreshLatestSnapshot → publish
- DjiDockTelemetryParserTest.java：新增缓存和重推测试
- .project-memory.md：记录


- **2026-07-30 14:20**：实现 state/event 变化即时重推。DjiDockTelemetryParser 缓存最近一次成功 OSD 的 topic+payload，新增 refreshLatestSnapshot() 方法供外部调用；DjiMqttConfiguration 在 parseState/parseEvent 后立即调用 refreshLatestSnapshot → publish，前端无需等 OSD 周期即可看到状态和告警更新。后端 mvn test 共 49 条全部通过。

### 当前任务：添加"模拟 DJI 连接"功能，驱动完整数据流水线

**目标**：新增后端 POST /api/v1/dashboard/integration/dji/demo 端点，构造一份逼真的 Dock 3 三连消息（OSD + state + events），通过真实解析流水线推送，前端自动切换为 DJI 模式。

**改动范围**：
- DroneStatusController.java：新增 demo 端点
- App.vue：DJI MQTT 卡片加"模拟 DJI 连接"按钮
- .project-memory.md / 开发日志.md：记录

